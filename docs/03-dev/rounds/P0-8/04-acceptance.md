# P0-8 — 人工验收清单

Round: P0-8
Status: AWAITING_ACCEPTANCE
**User Result: PASSED**
**Final Result: PASSED**
Reviewed By User At: 2026-09-24
Issues Found: 无
Git Commit: （**由用户本人提交**，待填）
Notes: 人工验收全部通过；`D4` / `D5` 两点未提出异议，按现有口径通过。

> ✅ **人工验收已于 2026-09-24 通过**（批次 `cmd_batch_p0-6.1_to_p0-9.md` 的统一验收）：
> 用户本人走完本文件的 A–G 七组并确认**全部通过**。日期与关键验收场景见文末
> 「验收日期 / 关键验收场景 / User Result / Final Result」。
> ⚠️ **`Status` 仍为 `AWAITING_ACCEPTANCE`，本轮不标 `DONE`**——「DONE 双门槛」的第二个条件
> （**用户本人完成 Git 提交**）尚未满足，四轮的改动至今全部躺在工作区，Claude 全程零 Git 写操作。

> ⚠️ **批次模式**：本轮不单独收口。`User Result` / `Final Result` 曾保持 `PENDING`，
> 由用户在 **P0-9 完成后**对 P0-6.1 → P0-7 → P0-8 → P0-9 四轮**一次性验收**——
> **该统一验收已于 2026-09-24 完成**，上方的 `PASSED` 即用户给出的结论（不是 Claude 自行判定）。
> 本清单仍然可以被单独执行——它只覆盖 P0-8 的内容。

---

## 零、准备

```bash
pnpm install          # 若尚未安装
pnpm dev              # :3000
```

⚠️ **Mock 存储写在 `globalThis` 上，重启 dev server 会全部重置。** 每条用例若中途重启过，请从该用例开头重做。
⚠️ 本轮的「到期自动通过」**不会自己发生**——没有真 Scheduler。要观察它，必须让**某次读取**触发
`sweepCompletionAutoApprovals()`。会触发的恰好是**四条读路径**（由「门禁 23」以集合相等钉死）：
**用户端订单**（列表 / 详情）、**管理端订单**、**客服端完成材料**（列表 / 详情）、
**打手端订单**（列表 / 详情——注意**列表也算**，列表卡片上的状态同样会过期）。
这是**有意**的，理由见 `02-decisions.md` D7。

**身份**（`ALLOW_MOCK_AUTH=true` 时，用户端右下角 DEV-1 面板可切换）：

| 身份 | 取得方式 |
|---|---|
| 用户 | DEV-1 面板切到任意 `u-*` |
| 打手 A（`cp-1`，`u-1001`） | DEV-1 面板的「打手」身份 |
| 打手 B（`cp-2`） | DEV-1 面板 |
| 客服 | `/staff/login` 用 `lib/constants/adminStaff.ts` 附近的 Mock 客服凭据 |

**预置数据参考**（`lib/mocks/fixtures/orderSeed.ts`）：
`ord-seed-1001-04`（`serving`，`cp-3`）、`ord-seed-1001-10`（`serving`，`cp-1`）。

---

## A. 打手提交完成材料

| # | 步骤 | 期望 |
|---|---|---|
| A1 | 用 `cp-1` 打开 `ord-seed-1001-10`（`serving`，本人）的详情 | 显示「提交完成材料」入口 |
| A2 | 说明填 **4 个字** 提交 | 被拒，提示说明需 **5～50 字** |
| A3 | 说明填 **51 个字** 提交 | 被拒，同上 |
| A4 | 说明填 5～50 字，不带凭证，提交 | 成功；页面进入「完成审核中」；**入口消失**（不得出现第二个提交按钮） |
| A5 | 刷新页面（不重新登录） | 仍是「完成审核中」；不出现重复提交入口 |
| A6 | 再用 `cp-1` 打开 `ord-seed-1001-04`（`serving`，**别人的**） | **没有**「提交完成材料」入口 |
| A7 | 用 `cp-2` 打开 `ord-seed-1001-10`（正在审核中，非本人也不是自己在履约） | 看不到任何提交入口，也看不到「审核中」被算成自己的 |
| A8 | 打开一张 `accepted`（未开始服务）的本人订单 | **没有**提交完成材料入口（`accepted → serving` 是 P0-7 的事） |
| A9 | 打开一张 `paid` / `completed` / `refunded` 的本人订单 | 都没有提交完成材料入口 |
| A10 | 用打手身份直接 `curl` 提交别人的单（`POST /api/companion/orders/ord-seed-1001-04/completion`，带请求体伪造 `companionId: "cp-1"`） | **403/404 级别的失败**，绝不是「按请求体里的 companionId 执行」 |

**A 组要确认的边界**：说明长度是真的按**字符数**（5～50）、不是按字节；凭证可以 0 个（选填）；
提交成功后订单状态在打手端**仍显示为 serving + 审核中**，不是 `completed`。

---

## B. 客服人工通过

| # | 步骤 | 期望 |
|---|---|---|
| B1 | 客服登录，进入「完成审核」 | 列表里能看到 A4 提交的那一份（订单号 + 打手名 + 提交时间 + 自动审核倒计时） |
| B2 | 打开详情 | 看得到完成说明、凭证（如有）、订单摘要、用户摘要 |
| B3 | 点「通过」 | 成功；该份材料离开待审列表 |
| B4 | 用打手身份刷新订单详情 | 订单为 **`completed`**；「完成审核中」消失；无「提交完成材料」入口 |
| B5 | 用用户身份打开同一订单 | 看到 `completed` |
| B6 | 管理端打开该订单 | 看到 `completed`，且 `completedAt` 有值 |
| B7 | 再次 `curl` approve 同一份材料 | **不产生第二次完成事实**，`completedAt` 不被刷新（幂等/安全失败） |
| B8 | 用打手身份 `curl` `POST /api/companion/orders/<上面那张单>/completion` | 被拒（已 `completed`，不得再提交改变既成事实） |

---

## C. 客服驳回 + 重新提交

| # | 步骤 | 期望 |
|---|---|---|
| C1 | 打手对另一张 `serving` 本人订单提交材料 | 成功，进入「完成审核中」 |
| C2 | 客服点「驳回」但**不填原因** | 被拒，提示必填驳回原因 |
| C3 | 填写原因后驳回 | 成功；材料变成「已驳回」 |
| C4 | 用打手身份刷新订单详情 | 订单**仍是 `serving`**；显示**驳回原因**；出现**重新提交**入口 |
| C5 | 客服侧该单不再出现在「待审」里 | 是 |
| C6 | 打手重新提交 | 成功；订单仍是 `serving`；新的倒计时**重新开始**（≈ 完整的 10 分钟，不是接着上一份的剩余时间） |
| C7 | 在客服端查看该订单的审核历史 | 能看到**两次**提交：一份 `rejected`（带原因、审核人、时间）+ 一份 `pending`；**旧的驳回记录未被覆盖或删除** |

**C 组要确认的边界**：驳回**不改变订单状态**、不影响金额、不产生任何收益。

---

## D. 10 分钟自动审核

> ⚠️ D 组需要**改配置**。默认 10 分钟，等不起，所以先把自动审核时长改成 **1** 分钟。

| # | 步骤 | 期望 |
|---|---|---|
| D1 | 管理员打开「平台参数」，确认有一个**自动审核时长**输入框，默认值 **10** | 是 |
| D2 | 把它改成 `1` 保存 | 成功 |
| D3 | 把某张 `serving` 本人订单提交完成材料（打手） | 成功，倒计时显示约 1 分钟 |
| D4 | **在等够 1 分钟之前**打开客服待审列表 / 打手订单详情 | 仍是 `pending`，**没有被自动通过**（deadline 未到） |
| D5 | 等过 1 分钟后打开客服待审列表（或打手订单详情 / 用户订单详情 / 管理端订单任一） | 该单自动变成 **`completed`**；材料不在待审列表里了 |
| D6 | 反复刷新页面 10 次 | 仍是 `completed`；`completedAt` **不变化**；不产生第二条完成记录 |
| D7 | 在管理端 / 客服端查看该材料的审核来源 | 明确显示为**系统自动通过**，且**没有**任何客服被记为审核人 |
| D8 | 把配置改回 `10`（或其它值）保存 | 成功；**已经 pending 的旧材料 deadline 不变**（见 D9） |
| D9 | 提交新的一份材料（另一张单）→ 改配置 → 再打开那张单 | 这一份的 deadline 按**提交时**的配置算，不随后台改动而变 |

---

## E. 阻塞：有投诉 / 有进行中退款时不自动通过

| # | 步骤 | 期望 |
|---|---|---|
| E1 | 配置改成 `1` 分钟 | 成功 |
| E2 | 用**用户**身份在某张 `serving` 订单上**提交一条投诉**（与该订单关联） | 成功 |
| E3 | 用打手身份在该订单提交完成材料 | 成功，进入 `pending` |
| E4 | 等过 1 分钟，反复打开客服待审列表 / 打手订单详情 | **仍是 `pending`**；订单**仍是 `serving`**；**客服打开该材料的详情页**能看到「为什么没自动通过」的说明，且说明能区分是**被投诉**挡住还是**被进行中的退款**挡住 |
| E5 | 客服**人工**点「通过」 | **允许**（人工审核不受阻塞限制）；订单 → `completed` |
| E6 | 另一张单：用户提交**退款申请**（进行中），打手提交完成材料，等过 deadline | 同样**不自动通过** |
| E7 | 把投诉处理完结（客服 resolve / close），再等过 deadline | 此时**可以**自动通过 |
| E8 | 用一条**已完结**的投诉做同样的实验 | 与 E7 一致：**不阻塞** |

> ⚠️ **E7 / E8 是本轮的一处产品口径取舍**，见 `02-decisions.md` **D4**：
> EX-COMPLETE-05 只写「有投诉 / 售后时：不得自动通过，保持人工处理」，未区分投诉是否已完结。
> 本轮取「**未完结**才算阻塞」。若产品认为「只要本单历史上出现过投诉就永久转人工」，
> **请在验收时明确说出**——改动点只有一处纯函数，不涉及数据结构。

---

## F. 状态枚举与「不越界」

| # | 步骤 | 期望 |
|---|---|---|
| F1 | 全站任何地方查看订单状态 | **只有** `paid / accepted / serving / completed / refunded` 五个值 |
| F2 | 搜索代码中的 `completion_review` | 只出现在**派生展示**的注释/文案里，**不在** `ORDER_STATUSES` 里 |
| F3 | 打手工作台 | **没有**收益中心 / 提现入口 / 冻结金额（P0-9 才有） |
| F4 | 打手端任意接口返回的 JSON | **不含**客服账号 id（`reviewedByStaffId` 之类） |
| F5 | 客服端完成材料详情返回的 JSON | 不含用户手机号/支付流水等无关内部字段 |
| F6 | 管理端「平台参数」页 | **只有两个**输入项（公共池超时 + 自动审核时长），没有多出第三套配置入口 |
| F7 | 任意页面（用户 / 打手 / 客服 / 管理）刷新后 | 没有出现「找不到接口」的报错；控制台没有 404 |

---

## G. 回归（前轮不能被本轮弄坏）

| # | 步骤 | 期望 |
|---|---|---|
| G1 | P0-7：`accepted` 的本人订单点「开始服务」 | 仍正常 → `serving`，`servingAt` 写入，重复点不刷新 |
| G2 | P0-6：`accepted` 主动取消 | 仍正常回公共池 |
| G3 | P0-6.1：打手工作台返回用户端入口 | 仍在 |
| G4 | P0-6.1：订单池「等待最久优先」 | 排序仍正确 |
| G5 | P0-5：派单 / 接单 | 仍正常 |
| G6 | P0-1：管理端「平台参数」只改公共池超时 | **仍然能保存**（本轮给配置表加了一个字段，旧表单只送一个字段不能被弄坏） |
| G7 | 用户端订单详情 / 我的订单 | 正常打开，状态显示正确 |

---

## 验收记录

| 组 | 结果 | 备注 |
|---|---|---|
| A 打手提交 | ☑ PASS / ☐ FAIL | 2026-09-24 |
| B 客服人工通过 | ☑ PASS / ☐ FAIL | 2026-09-24 |
| C 客服驳回 + 重提 | ☑ PASS / ☐ FAIL | 2026-09-24 |
| D 10 分钟自动审核 | ☑ PASS / ☐ FAIL | 2026-09-24 |
| E 阻塞（投诉 / 退款） | ☑ PASS / ☐ FAIL | 2026-09-24 |
| F 状态枚举与不越界 | ☑ PASS / ☐ FAIL | 2026-09-24 |
| G 回归 | ☑ PASS / ☐ FAIL | 2026-09-24 |

**待产品确认的两点（不阻塞验收，但请明确表态）**

1. **D4 的阻塞口径**：已完结（`resolved` / `closed`）的投诉是否仍然阻塞自动审核？本轮取「不阻塞」。
2. **D5 的配置上下限**：自动审核时长沿用平台时长的 `1～1440` 分钟上下限（复用同一个数字，未发明新数字）。
   若产品另有量级要求，改动点只在常量层一处。

> 📌 上述两点在 2026-09-24 的统一人工验收中**未提出异议**：按现有口径（D4 不阻塞、D5 沿用 `1～1440`）验收通过。
> 若日后要改，改动点已如上述，只在纯函数 / 常量层一处，不动数据结构。

---

## 验收日期

**2026-09-24**（本文件处于批次 `cmd_batch_p0-6.1_to_p0-9.md`，四轮由用户在 P0-9 完成后**一次性统一验收**）

## 关键验收场景（本轮）

用户本人于 2026-09-24 走完本文件的 A–G 七组验收并确认**全部通过**。覆盖的关键场景：

| 组 | 场景 | 结果 |
|---|---|---|
| A | 打手在**本人 `serving` 订单**上提交 5～50 字说明 + 凭证；提交后订单**仍是 `serving`**，展示为派生的「审核中」（`completion_review` **不进 `OrderStatus`**） | ✅ PASS |
| A | 校验：说明少于 5 字 / 超过 50 字被拒；**非本人**、**非 `serving`** 的单提交被拒 | ✅ PASS |
| B | 客服人工 **approve**：`submission → approved` + 订单 → **`completed`**（`completedAt` 写入），审核人 / 审核时间保留 | ✅ PASS |
| C | 客服 **reject**：`submission → rejected`、**订单保持 `serving`**、驳回原因与审核人可查；打手可**重新提交**且**不覆盖旧审核历史** | ✅ PASS |
| D | **到期自动通过**：`pending` + `autoApprovalDeadlineAt` 到点 + 订单仍 `serving` + 无阻塞 → `approved` + `completed`，来源记为 **System**（**不伪装成客服人工审核**） | ✅ PASS |
| E | **阻塞**：本单存在**进行中**的投诉 / 退款时**不自动通过**；（按 `D4` 口径，已 `resolved` / `closed` 的投诉不阻塞） | ✅ PASS |
| F | **状态枚举与不越界**：全站订单状态**恰好五个值**（`paid / accepted / serving / completed / refunded`）；`completion_review` 只在派生展示层；打手端**没有**收益 / 提现入口；打手端 DTO **不含**客服账号 id；客服端详情不含无关内部字段；管理端平台参数**只有两个**输入项 | ✅ PASS |
| G | 回归：P0-7 开始服务、P0-6 取消回池、P0-6.1 返回入口与池排序、P0-5 派单接单、P0-1 旧表单只送一个字段仍能保存、用户端订单详情 | ✅ PASS |

## Issues Found

无（本轮验收未发现新问题）

## User Result

**PASSED**

## Final Result

**PASSED**

## Git Commit

（**由用户本人提交**——Claude 不执行任何 Git 写操作；本字段由用户在提交后填写）

> ⚠️ **本轮状态仍为 `AWAITING_ACCEPTANCE`，不标 `DONE`。**
> 按 `development-workflow.md` §十七 的「DONE 双门槛」，两个条件必须**同时**满足：
> ① 用户本人说明验收通过 —— ✅ 已于 2026-09-24 满足；
> ② 用户本人完成该项目相关的 Git 提交 —— ☐ **尚未满足**。
> 因此在用户本人提交之前，本轮停在 `AWAITING_ACCEPTANCE`。
> ⚠️ `Status: AWAITING_ACCEPTANCE` 与 `Accepted At: 2026-09-24` **并不矛盾**：
> 前者是「Round 是否收口」的唯一状态字段，后者记录人工验收通过的时间。
