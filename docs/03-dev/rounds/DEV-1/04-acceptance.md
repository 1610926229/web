# Acceptance

> Round: `DEV-1` — Mock 身份切换验收工具
> Status: `DONE` — `User Result` / `Final Result` = `PASSED`（2026-09-24）· `Git Commit: 53481ea`
> ⚠️ 本轮**不是产品功能**，验收标准是「这个开发工具好用且不越界」，不是「业务行为对不对」。
> ⚠️ 本轮**不改 P0-6 的任何行为，也不改 P0-6 的验收结果**；P0-6 的验收由该轮自己的
> 验收记录（`../P0-6/04-acceptance.md`）承载。
>
> ⚠️ **本文件已按重做批改写（首轮验收被打回之后）。** 首轮版本要求验收人先开管理端标签页
> 「通过两条申请 + 恢复接单」才能造出打手，那是被驳回的读法。现在**预置数据里直接就有两位
> 有效打手**，§G 全程**不需要任何后台操作**。

---

## Manual Acceptance Checklist

前置：`pnpm dev`（`.env.local` 里 `ENABLE_MOCK_AUTH=true`），手机模拟器宽度看。

### A. 四道自动闸门（先做这步）

| # | 命令 | 预期 |
|---|---|---|
| 1 | `pnpm test` | `tests 1116` / `pass 996` / **`fail 0`** / `skipped 120` |
| 2 | `pnpm build && pnpm start` 后 `APP_BASE_URL=http://localhost:3000 pnpm test` | `1116 / 1116` / **`fail 0`** / **`skipped 0`** |
| 3 | `pnpm typecheck` | 退出码 `0`（首次需先跑一次 `next typegen`，否则 `PageProps` 之类会报未定义） |
| 4 | `pnpm lint` | 退出码 `0` |

### B. A —— 工具存在，且知道当前是谁（`01-prompt.md` §十四 A）

5. 打开用户端任意页面（`/` 或 `/mine`）。预期：**右下角、底部五 Tab 之上**有一个
   `Mock 身份（开发工具）` 按钮，**不遮挡 Tab 栏**，按钮上能看出当前身份（游客时显示「游客」）。
   ⚠️ **这一条请在带 Home Indicator 的真机（或微信内浏览器）上再看一眼**：面板的抬升高度
   要把底部安全区算进去（`env(safe-area-inset-bottom)`），而桌面模拟器里 `env()` 恒为 0，
   真机上 TabBar 会比我这一层看到的高一截。审查阶段正是从 CSS 里推断出这个位置会压住
   TabBar 顶部，已按仓库其它贴底元素同一算法修正——但**修正结果只有真机看得出来**。
   顺手在结算页（底部有「去支付」那条栏）也确认一下没有盖住它。
6. 点开。预期：出现一张卡片，含
   - 标题 `Mock Identity（开发工具）`；
   - `当前身份：<昵称>（<userId>）`；未登录时是 `游客（未登录）`；
   - 说明句，讲清「点账号会**替换**当前会话」；
   - **8 个测试账号**，每个带申请状态与用途标注；
   - `退出登录` 与 `收起`。

### C. 三种身份**一眼可辨**（重做批新增，本轮最该看的一条）

7. 在展开的名单里核对**资格标签**（每条账号名右侧的小胶囊）：

   | 账号 | 昵称 | 预期标签 |
   |---|---|---|
   | `u-1001` | 老板A（占位） | **普通用户** |
   | `u-1022` | 夜航（占位） | **有效打手** |
   | `u-1023` | 栖迟（占位） | **有效打手** |
   | `u-1002` / `u-1003` / `u-1008` / `u-1009` / `u-1010` | — | **普通用户** |

   ⚠️ 这三个标签**由服务端现算**（`resolveCompanionAccess`），不是名单里写死的文案。
   验收时请顺手确认「标签与点进去的结果一致」——尤其切到 `夜航` 后 `/companion` 真的放行。
   若出现「标着有效打手、进去却被 403」，那是**必须记下的 Bug**。

   同时确认**当前身份那一行有视觉区分**（提示词 §六 的要求）：该行有蓝色描边，
   右下角写「当前身份」而不是「登录」。切换成功后这个标记要跟着换行。
   ⚠️ 标记只是显示：**那一行照样可以点**（点它等于以同一身份重新登录），不置灰、不挡点击。

8. 标签**只是显示**：请在点 `夜航` 之前之后各点一次普通用户，确认标签没有任何
   禁用 / 锁定 / 阻止点击的效果（面板不拿它当权限）。

### D. B —— 连续切换（§十四 B）

9. 依次点 `老板A（占位）` → `夜航（占位）` → `栖迟（占位）` → 回到 `老板A（占位）`。
   预期：**四次都成功**，每次点完页面自动刷新，折叠按钮上的昵称跟着变。
   ⚠️ 不需要先退出，也不需要开无痕窗口——这正是本工具存在的理由。

### E. C —— 权限按真实规则变化（§十四 C）

10. 切到 `老板A（占位）`（`u-1001`，标签「普通用户」）：
    - `/mine`：正常显示我的信息；
    - `/companion`：显示**不是打手**那一档（预期文案属于「不是护航」类），**进不去工作台**；
    - `/companion/orders`：**404 或等价的不放行**。
11. 切到 `夜航（占位）`（`u-1022`，标签「有效打手」）：
    - `/companion`：**放行**，进入工作台；
    - `/companion/pool`：进得去，且**公共池有可接的单**（不是「进得去但没有单」）；
    - `/companion/orders`：进得去。
    ⚠️ **这一条不需要先去后台把它审核成打手**——它一启动就是。
12. 切到 `栖迟（占位）`（`u-1023`）：与第 11 条同样放行。

### F. D —— 退出后恢复游客（§十四 D）

13. 点 `退出登录`。预期：页面刷新后回到游客态——`/mine` 显示「需要登录」，
    折叠按钮显示 `游客（未登录）`，`/companion` 不再放行。
14. 退出这一步走的是现有的 `POST /api/auth/logout`（**服务端下发清空 Cookie**），
    不是客户端自己删——`tests/devIdentity.test.mjs` 的 HTTP 用例断言了响应里带
    `Max-Age=0` / 已过期的 `mock_user_id`。

### G. E —— 开关关闭时工具不存在（§十四 E，本轮**最重要的安全约束**）

15. **不要改 `.env.local`**，用进程环境变量覆盖后重启：
    ```bash
    ENABLE_MOCK_AUTH=false pnpm dev
    ```
    （Next 不会覆盖已存在的进程环境变量，因此这一条能生效。）
16. 打开 `/`、`/mine`、`/help`。预期：**页面上找不到 `Mock 身份` 或 `Mock Identity`**
    （不是灰掉、不是禁用——是**不存在**）。
17. 手工确认名单没有漏出去：页面源码里搜 `u-1001`，预期 **0 处**；
    再搜 `有效打手`（重做批新增的标签文案），预期也是 **0 处**。
    （关闭态下早返回发生在读会话与算标签之前，因此名单与标签都进不了响应。
    ⚠️ **开关打开时则相反**——8 个 id 会作为 `accessLabels` 的键出现在 RSC payload 里，
    这是开发形态的既成事实，不必当成 Bug，见 `03-delivery.md` §6。）
18. `curl -i -X POST http://localhost:3000/api/auth/mock-login` 预期 **404**；
    `curl -i -X POST http://localhost:3000/api/auth/logout` 预期 **404**。
    （接口层既有的保护**没有被本轮削弱**。）
19. 手工把 `mock_user_id=u-1002` 塞进 Cookie 再访问 `/mine`。预期：仍然「需要登录」。

### H. §十一 —— 用它跑一遍 P0-6 的完整多角色链路（**本工具的验收理由**）

> 以下是 P0-6 的人工流程，**在 DEV-1 之前需要开多个浏览器 / 无痕窗口**，现在一个窗口就够。
> 全程**只用**右下角的面板换身份，不要开无痕窗口。
>
> ⚠️ **本段全程不需要进管理端、不需要通过任何申请、不需要「恢复接单」。**
> 首轮版本在这里要求先做一遍审核，被人工验收驳回——预置数据现在直接带两位有效打手。
> 若你发现自己想去后台点「通过」，说明哪里不对，请记下来。
>
> ⚠️ DEV-1 只提供切换工具。**若这一步发现 P0-6 的 Bug：记录下来即可，不属于 DEV-1 的修复范围**
> （除非它能证明是 DEV-1 引入的回归）。

角色：`U = 老板A（占位）u-1001` · `A = 夜航（占位）u-1022` · `B = 栖迟（占位）u-1023`。

20. 面板切到 `U` → 下单并支付，得到一张待在公共池被接的订单；记下订单号。
21. 面板切到 `A` → `/companion/pool` 找到该订单 → **接单**。
    预期：接单成功后它会出现在 `/companion/orders`。
22. 仍在 `A`：打开 `/companion/orders/<id>` → **取消接单**（填原因）。
    预期：按 P0-6 的既有行为走完，本工具不改变其中任何一步。
23. 面板切到 `B` → `/companion/pool`。
    预期：**这张单重新出现在公共池**（P0-6 的 `accepted → paid → public`），且能被 B 接走。
    ⚠️ 公共池的接单资格**不按游戏 / 大区 / 服务标签过滤**（只有「专属池是不是指定给我」
    这一条比对），所以 B 与 A 打的游戏不同**不影响**这一步——这是读源码确认的，不是猜测。
24. 面板切回 `U` → 订单详情与通知。
    预期：能看到「打手取消接单 → 重新进入公共池 → 被新的打手接走」的完整结果，
    通知里有对应的三条（接单 / 取消接单 / 再次接单）。

### I. 一条已知的边界（不是 Bug，是范围）

25. **在打手工作台里换身份，要先回到任一用户端页面。**
    面板只挂在用户端路由组（`app/(mobile)/layout.tsx`），不进管理端、客服端、打手工作台。
    理由：§十五 的禁止清单含「Admin / Staff 身份切换器」，且 §十二 只要求「接入一个合适的
    现有 layout」；挂到第二个端属于扩大范围。**同一个浏览器、不需要无痕窗口**，
    回到用户端页面点一下再走进工作台即可。
26. `MockUserPicker` 在**登录界面**上仍然存在（设置页退出之后会落到那里）。
    预期：两个入口**共用同一份名单与同一条登录链路**，不是两套。这一条由
    `tests/mockUsers.test.mjs` 的宿主清单断言保护。

---

## User Result

<!-- 验收结论写在下面（保留历史，不要覆盖） -->

**PASSED —— 2026-09-24 用户人工验收通过。**

✅ **`DEV-1 DONE — implementation committed in 53481ea`**

按 `development-workflow.md` §十七「DONE 的双重门槛」，两个条件**均已满足**：
用户本人说明人工验收通过（本次）**且** 用户本人完成 Git 提交（`53481ea`）。
`User Result` / `Final Result` 保持 `PASSED`。

本轮验收覆盖了两件事：

1. **工具本身**（§B–§G）：开关关闭时面板**不存在**（不是禁用）、名单与资格标签不进响应、
   三个端口的认证接口与 Cookie 数量一个没多；
2. **它存在的理由**（§H）：在**同一个窗口**里用面板跑完
   `老板A u-1001 下单 → 夜航 u-1022 接单 → 取消 → 栖迟 u-1023 抢单 → 切回老板A 看通知`，
   **全程没有进管理端、没有通过任何申请、没有点「恢复接单」**——这正是重做批要证明的事。

## Issues Found

**本轮（开发工具）本身没有发现缺陷。**

验收期间另外发现的两个问题属 **P0-6 域**，不是本工具的缺陷，已记在
`P0-6/04-acceptance.md` 的 `Issues Found`，并在 `docs/03-dev/总需求进度表.md`
登记为两条独立的 `NEEDS_FIX` 待办：

- **FIX-1** 打手工作台缺少返回普通用户主界面的入口；
- **FIX-2** 订单池排序应改为「等待最久优先」（`publicPoolEnteredAt` / `exclusiveEnteredAt` ASC）。

⚠️ 两者都不推翻本轮验收结论。

## Rework

发生过一次返工，记录在 `02-decisions.md`（D3 V1→V2、D6 V1→V2）与 `03-delivery.md` §2.5：

> 首轮交付被人工作为**需求未达成**驳回——工具能切 User，却跑不完 P0-6 的
> User → Companion A → Companion B 链路。重做批在既有 fixture 体系里**真的预置**了两位
> 有效打手（`cp-10`/`u-1022`、`cp-11`/`u-1023`），验收链路从此不需要任何后台审核动作。

## Final Result

**PASSED**

## Git Commit

`53481ea`（**用户本人提交**）

> **`DEV-1 DONE — implementation committed in 53481ea`**
