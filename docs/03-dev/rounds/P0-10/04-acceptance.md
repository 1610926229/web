# P0-10 — 人工验收记录

Round: P0-10
**Status: DONE**（2026-09-25，按用户明确指令收口；⚠️ 含一处**协议偏离**，见下方声明）
**User Result: PASSED**（2026-09-25，用户本人整体确认「P0 人工验收已通过」）
**Final Result: PASSED**
Accepted At: 2026-09-25
Issues Found: 无（用户未提出新的验收问题；本轮交付前的整改 A0 已完成并单独记录）
Git Commit: ——（**无 commit hash 可填**：截至 2026-09-25 本批次四轮改动**仍在工作区未提交**，`HEAD` 仍为 `3fbae62`）
Recorded At: 2026-09-25（验收 + 收口）

> ⚠️ **本轮的 `DONE` 含一次「经用户明确授权的协议偏离」，如实记录如下——不静默改。**
> `development-workflow.md` §十七「DONE 的双重门槛」规定 `Status = DONE` 需要**同时**满足：
> ① 用户本人说明验收通过；② **用户本人完成该项目相关的 Git 提交**。
> 2026-09-25 用户本人确认 **① 人工验收通过**，并**明确指令**将本轮标为 `DONE`；
> 但 **② 的提交当时尚未发生**——本批次四轮的改动都还躺在工作区（`git status` = 25 `??` / 76 `M`，
> `HEAD` = `3fbae62`）。**因此这一行不是「双门槛已满足」，而是「按用户明确指令收口」。**
> 依协议本应停留在 `AWAITING_ACCEPTANCE` 并在 `Next Step` 写「等待用户本人 Git 提交」；
> 该偏离由**用户本人**作出，**不是 Claude 自行放宽**——Claude 全程**零 Git 写操作**，
> 也**未代填任何 commit hash**。📌 **用户完成提交后，请把上面的 `Git Commit` 补成真实 hash。**

> 📌 **验收形态的如实说明**：用户以**整体一句话**确认「P0 人工验收已通过」，
> **没有**逐组打勾，也没有填写 §四 表里的逐项结果。因此 §四 的分组行保留 `——`，
> 下方收口块记录的是**用户的整体结论**，不是伪造的逐组证据。

> ✅ **自动门禁已全部通过**（读数见 `03-delivery.md` §六，为**复核整改之后**的最终读数）：
> `pnpm test` **1278** / fail 0 · 生产 `APP_BASE_URL` 全量 **1278/1278 / fail 0 / skipped 0** ·
> typegen + `tsc --noEmit` exit 0 · `eslint` 0 error 0 warning · `next build` exit 0。
>
> ✅ **只读 reviewer 复核已结束**（`03-delivery.md` §七）：**BLOCKER 0 / MAJOR 1 / MINOR 4 / NOTE 7**。
> 那 1 条 MAJOR 是**回归保护缺失**（两条新接口当时没有任何 HTTP 级测试），reviewer 已手工确认
> **行为本身都是对的**；MAJOR 与 4 条 MINOR **已全部修完并复跑门禁**，
> 新增 5 条 HTTP 用例并做过受控 mutation 证明它们真的会红（§7.3）。
> 因此本文件 **A6 / A7 / D12 / D13** 四条现在是**有测试钉住的**，不再是「验收时手测一遍就算过」。

> ⚠️ **本文件第 1 条验收项是一个需要产品负责人裁定的口径**（`02-decisions.md` §九 **A9-9**），
> 它**不是本轮引入的缺陷**，是**既有四个客服页面共同的缺口**。请先看 §二 A0。

---

## 一、验收前置（不满足则下面的步骤全部无意义）

| # | 前置 | 命令 / 说明 |
|---|---|---|
| P1 | dev server 在跑，且 Mock 登录开启 | `pnpm dev`；`.env.local` 需 `ENABLE_MOCK_AUTH=true` |
| P2 | 浏览器设成手机 / 平板模拟器宽度 | 工作台是**桌面优先**的（不是 480px 的手机列），但也要能用窄屏 |
| P3 | 需要 **3 个会话** | Tab1 = 用户（`u-1001` 老板A）· Tab2 = 客服（`/staff/login` 选一位客服）· Tab3 = 管理端（`/admin/login`） |
| P4 | 自动门禁已绿 | 见 `03-delivery.md` §六 |
| P5 | **E 段**需要能改后台平台参数 | Tab3 → `/admin/platform-config`；`publicPoolTimeoutMinutes` 取值 **1~1440**，默认 **60** |

> Mock 数据是 `globalThis` 内存 store，**重启 dev server 会重置**。E 段有等待，中途不要重启。

---

## 二、验收清单

### A. 入口与权限（**改 URL 试，不是点页面**）

| # | 操作 | 预期 | 需求依据 |
|---|---|---|---|
| **A0** | ✅ **已裁定并已修复**（2026-09-25，`02-decisions.md` §九 **A9-11**）。**验一下真的搜得到了**：Tab2 客服 → `/staff/orders` → 在搜索框里输入**用户资料页上那串 UUID**（`u-1001` 的 `displayId` = `3f2a9c14-6b7d-4e58-9c21-8d4f0b7a5e63`） | **能搜到该用户的订单**（修复前是「搜不到任何订单」）。同一张单用**旧的内部标识** `u-1001` 搜**仍然搜得到**——`displayId` 是**新增的一路**，不是替换 | `cmd_p0-10.md` L12「用户昵称 / 平台 ID 搜索」；`lib/types/user.ts:37`（`displayId` 存在的理由就是「客服页面报单号时用它对人」） |
| A1 | Tab2 客服 → 顶栏导航 | 有「**订单**」入口，且**排在「工作台」之后、「会话」之前** | `StaffHeader.tsx` `NAV_ITEMS` |
| A2 | 点「订单」 | 打开 `/staff/orders`，是列表页（**不是**空白、不是 403） | `cmd_p0-10.md` L12 |
| A3 | **页面** `/staff/orders`，用**无 Cookie 的隐身窗口** | **307 → `/staff/login`**（页面是跳登录，不是 401；401 是**接口**的行为，见 A6） | 权限守卫 |
| A4 | **页面**同一 URL，只带**用户端 Cookie**（普通用户 `u-1001`） | 同样 **307 → `/staff/login`**（客服端与用户端是两套 Cookie，没有客服会话就是「没登录」） | 四套身份不合并 |
| A5 | **页面**同一 URL，只带**管理端 Cookie** | 同样 **307 → `/staff/login`** | 同上 |
| A6 | 直接访问**接口** `/api/staff/orders`：无 Cookie / 只带用户端 Cookie / 只带管理端 Cookie | 三种都是 **401** | 服务端鉴权 |
| A7 | 对**接口** `/api/staff/orders` 发 `POST` | **405**（该接口只导出 `GET`，客服**没有**写入口） | `cmd_p0-10.md` L16 |

> 上面这张表已在**生产构建**上实测过（`next start`，2026-09-24），读数如下——验收时应当复现同样的数字：
>
> | 目标 | 凭据 | 实测 |
> |---|---|---|
> | `/staff/orders`（页面） | 无 / 用户端 / 管理端 | **307 → `/staff/login`** |
> | `/staff/orders`（页面） | 客服 | **200** |
> | `/staff/orders/<存在的单>`（页面） | 客服 | **200** |
> | `/staff/orders/nope`（页面） | 客服 | **404** |
> | `/api/staff/orders` | 无 / 用户端 / 管理端 | **401** |
> | `/api/staff/orders` | 客服 | **200** |
> | `/api/staff/orders` + `POST` | 客服 | **405** |
> | `/api/staff/orders/nope` | 客服 | **404** + `{"error":{"code":"NOT_FOUND","message":"订单不存在"}}` |

### B. 列表：搜索与筛选

| # | 操作 | 预期 | 需求依据 |
|---|---|---|---|
| B1 | 搜索框输入**一个订单号**（如 `YMRANKTODAY1011`） | 只剩那一单 | `cmd_p0-10.md` L12 |
| B2 | 输入**用户昵称的一部分**（如 `老板A`） | 出现该用户的订单；**每一行的用户列显示得出昵称**（命中理由必须看得见） | 同上 |
| B3 | 输入**平台标识** `u-1001` | 出现 `u-1001` 的订单（与 A0 对照看） | 同上 |
| B4 | 输入**商品名的一部分**（如 `机密`） | 出现商品名包含它的订单 | 同上 |
| B5 | 输入一个**绝不存在的关键词**（如 `zzzz`） | 空列表 + **「当前筛选下没有订单」**的说明文案，**不是**报错、不是白屏 | 空态 |
| B6 | 只输入**空格** | 等同于没有搜索（返回全部） | 关键词只去空白 |
| B7 | 状态筛选选「已完成」 | 只剩 `completed` 的订单 | `cmd_p0-10.md` L12 |
| B8 | 游戏筛选选一个具体游戏 | 只剩该游戏的订单；**筛选栏里仍有全部游戏选项**（不因为筛了状态就只剩一个） | —— |
| B9 | 时间范围填**今天** | 只出现创建时间在北京时间今天之内的订单 | 排序与筛选算**同一个字段**（`createdAt`） |
| B10 | 时间范围填一个**开始晚于结束**的区间（如 `2026-09-10` ~ `2026-09-01`） | 接口层 **400**；页面上是「没有订单」而不是崩掉（页面走宽松模式） | `readOrderFilterDate` |
| B11 | 组合：状态 + 游戏 + 时间 + 关键词 | 四个条件**同时生效**，不是只取最后一个 | —— |
| B12 | 清空全部筛选 | 回到全部订单 | —— |
| B13 | 先筛到只剩 1 条，再点**页头刷新** | **筛选条件与结果一起重置**（回到不筛选的全部订单），地址栏也随之清干净——`total`、页码、筛选三者不能各说各话 | reviewer NOTE 7：测的是「状态与地址栏同步」这条容易被忽略的路径 |

### C. 列表：排序与分页

| # | 操作 | 预期 | 需求依据 |
|---|---|---|---|
| C1 | 不筛选，看列表顺序 | **创建时间倒序**（最上面是最新创建的那一单） | 共享的 `compareOrdersByCreatedAt` |
| C2 | 看「共 N 条」的数字，与筛选后的条数比对 | 「共 N 条」是**筛选之后**的条数；翻到最后一页时**没有空页** | `total` 必须描述过滤后的集合 |
| C3 | 一直翻到最后一页 | 最后一页的「下一页」消失（`hasMore=false`） | —— |
| C4 | 翻页过程中检查 | **没有一条订单在两页里各出现一次** | 偏移量分页的稳定性 |

### D. 详情：这一单长什么样、做过什么

| # | 操作 | 预期 | 需求依据 |
|---|---|---|---|
| D1 | 从列表点任意一行 | 打开 `/staff/orders/<订单号>` | `cmd_p0-10.md` L14 |
| D2 | 看页面 | 有：订单号 · 状态 · 用户摘要（昵称 + 头像）· 商品 / 规格 / 数量 / 增值服务 · **四个金额**（原价 / 优惠 / **实付** / **已退**）· 当前打手 · 时间线 | 同上 |
| D3 | 看金额区 | 是**只读展示**，**没有任何输入框或按钮**能改金额 | 客服无处置权 |
| D4 | 看金额区的说明文案 | 明确写出「金额由服务端计算，本页只做展示」以及**本页不展示分账比例与平台收入** | 用户权限表 §7.2 |
| D5 | 找一个**有派单记录**的订单 | 「派单进度」显示：在哪等 / 等到什么时候 / 结果 | `toStaffOrderDispatchSummary` |
| D6 | 找一个**没有派单记录**的历史订单 | 派单区**不报错**（显示为「没有」而不是空白或崩溃） | 历史数据 |
| D7 | 找一个**有退出历史**的订单（打手取消过的） | 「履约退出历史」列出：谁、什么方式、什么原因、什么时候 | P0-6 |
| D8 | 找一个**没有退出历史**的订单 | 退出历史是**空**，不是一句「查询失败」 | —— |
| D9 | 找一个**有退款申请**的订单 | 显示退款摘要，可点进退款详情 | 复用 `StaffRefundListItem` |
| D10 | 找一个**有投诉**的订单 | 显示投诉摘要（一单可以有多条） | 复用 `StaffComplaintListItem` |
| D11 | 找一个**提交过完成材料**的订单 | 显示完成材料摘要（到哪一步了） | 复用 `StaffCompletionListItem` |
| D12 | 打开一个**不存在的订单号**（如 `/staff/orders/nope`，带客服 Cookie） | **404 页面**，**不是** 200 的空白页、不是 500 | `notFound()` |
| D13 | 直接访问 `/api/staff/orders/nope`（带客服 Cookie） | **404** + `{"error":{"code":"NOT_FOUND","message":"订单不存在"}}` | `STAFF_ORDER_NOT_FOUND_MESSAGE` |

### E. **超时事实的惰性物化**（本轮的 `02-decisions.md` A9-4 这一条只有这里能真验）

> 这一段的目的是证明：**客服看到的订单状态不会停留在已经过期的事实上**。

| # | 操作 | 预期 | 需求依据 |
|---|---|---|---|
| E1 | Tab3 管理端 → `/admin/platform-config` → 把**公共池超时**改成 **1** 分钟 → 保存 | 保存成功 | P0-1（复用） |
| E2 | Tab1 用户 `u-1001` 下一单，**不指定打手** → 支付成功 | 订单进入 `paid`，派单进公共池，1 分钟后到期 | P0-5 |
| E3 | **不要打开任何订单页面**，静默等待 ≥ 1 分 30 秒（不要重启 dev server） | —— | —— |
| E4 | 等满之后，Tab2 客服**直接打开 `/staff/orders/<这一单的订单号>`**（**先开详情、不要先开列表**） | 状态是「**已退款**」，派单进度是「**已关闭**」——**不是**「公共池等待接单」 | **A9-4**（详情也必须挂物化：客服往往是直接按订单号打开这一页） |
| E5 | 再打开 `/staff/orders` 列表，找到同一单 | 列表里也是「已退款」 | A9-4（两条读路径各自都挂着） |
| E6 | E4 之后把公共池超时改回 **60** | 保存成功 | —— |

> ⚠️ **E4 的关键是「先开详情、不要先开列表」**：先开列表会让列表的物化把状态改掉，
> 那样验的就只是列表那一条路径了。两条路径**各自**都挂着是本轮的实际实现。
>
> 📌 **E4 里的派单状态名已按 P0-12 同步为「已关闭」**（原先写的是「超时关闭」）。
> P0-12 让「订单在开始服务前被用户直接退款」也会关闭派单，而这个状态还有原本那一种
> 来路（公共池到点无人接），因此标签改成了中性的「已关闭」，否则客服会把用户取消的单
> 说成无人接单。**E4 要验的性质没有变**：这一行**不再是**「公共池等待接单」。

### F. 只读边界：页面上**没有**任何处置动作

| # | 操作 | 预期 | 需求依据 |
|---|---|---|---|
| F1 | 把订单页从列表翻到详情，**逐屏找按钮** | **没有**「换打手」「重新进入公共池」「拒绝」「通过」「退款」「改金额」中的**任何一个** | `cmd_p0-10.md` L16 |
| F2 | 找一个 `paid`（等待接单）的订单，在详情页上找 | 同样**没有**任何能改变这一单的动作 | 同上 |
| F3 | 找一个 `serving` 的订单，在详情页上找 | 同样没有 | 同上（那些是 P0-11 / P0-12 / P0-13） |

### G. DTO 隐私（**看响应，不是看页面**）

| # | 操作 | 预期 | 需求依据 |
|---|---|---|---|
| G1 | 带客服 Cookie 打开 `/api/staff/orders?pageSize=1` | 响应里**没有** `clubNetIncome` / `companionRateSnapshot` / `companionBaseIncome` / `platformIncome` | 用户权限表 §7.2 |
| G2 | 同一响应 | **没有** `gameAccountId` / `remark` / `userId` / `idempotencyKey` | DTO 最小化 |
| G3 | 带客服 Cookie 打开 `/api/staff/orders/<有派单的订单号>` | 派单摘要里**没有**两个 `*CompanionId`，也**没有** `publicTimeoutMinutesSnapshot` | `StaffOrderDispatchSummary` 的字段表 |
| G4 | 同一响应 | 用户摘要只有 `id` / `nickname` / `avatarUrl` 三个字段 | `StaffUserSummary` |
| G5 | 在页面上搜索「分账」「平台收入」「净收入」等字样 | **页面上找不到**（不只是响应里没有） | 同上 |

### H. 回归（确认没有把前几轮弄坏）

| # | 操作 | 预期 |
|---|---|---|
| H1 | Tab3 管理端 → `/admin/orders` | 列表、筛选、分页、排序**与 P0-10 之前完全一致**（本轮的 `orderFilters.ts` 是纯上移，管理端行为逐字不变） |
| H2 | Tab1 用户端 → 我的订单 → 任意一单详情 | 正常 |
| H3 | Tab2 客服 → `/staff/conversations`、`/staff/refunds`、`/staff/complaints`、`/staff/completions` | 四个页面都正常；**退出历史**仍然显示得出 |
| H4 | 走一遍 P0-6 / P0-7 / P0-8 的主链路（接单 → 取消回池 → 再抢 → 开始服务 → 提交完成材料 → 客服通过） | 与之前一致 |
| H5 | 完成一单后，Tab1 打手 → `/companion/earnings` | 收益记录正常（**冻结中**） |

---

## 三、本轮的已知限制（**不是缺陷，是有意为之**，列出来免得被当 bug 报）

| # | 限制 | 原因 / 归属 |
|---|---|---|
| 1 | ~~客服搜不到用户的「平台展示 ID」~~ | ✅ **已解除**（2026-09-25）。产品负责人裁定选**方案 B**：`/staff/orders` 新增 `displayId` 一路（既参与搜索、也可展示）。见 §九 **A9-11**。**变成 A0 的验收动作**，不再是限制 |
| 2 | 客服端**没有任何**改订单的能力 | 本轮边界（`cmd_p0-10.md` L16）；换人是 P0-11、退款是 P0-12 / P0-13 |
| 3 | 超时 / 自动通过是**读路径上的惰性物化**，不是后台任务 | 仓库**没有 Scheduler**。真实 Scheduler 是**上线前 production blocker**，不是本轮遗漏；届时要调用**同一个**函数 |
| 4 | `?mockEmpty=orders` **未接线** | A9-10（管理端同类页面有这条调试开关）。不影响任何业务行为 |
| 5 | 详情页**没有**「加载中」骨架 | **刻意**：详情目录之上不能有 `loading.tsx`——加载边界会先以 200 发出外壳，迟到的 `notFound()` 就改不了状态码（`tests/staff.test.mjs` 有一条走查到 `app/` 的门禁钉着这件事） |
| 6 | ~~`tests/platformConfig.test.mjs` 有**偶发**~~ | ✅ **已解除**（P0-12，2026-09-25）。**根因不是「偶发」**，是一条本来就写错的断言（毫秒级时钟精度），已修复且命题未变。见 §九 **A9-12** |
| 7 | `?mockEmpty=orders` **未接线** | A9-10（管理端同类页面有这条调试开关）。不影响任何业务行为。见上一条 |
| 8 | 两个惰性物化里，`sweepCompletionAutoApprovals` 有「必须挂在全部读取路径上」的**门禁**，`sweepExpiredDispatches` **没有同等门禁** | 现状即如此，**本轮不顺手加**（那会是一处与本轮无关的新结构约束）。本轮的两条物化用例**两个 sweep 都真验了**，因此本轮的挂点是有保护的；这里说的是「全仓所有调用方」没有门禁 |
| 9 | ~~页面上**看不到「平台展示 ID」**~~ | ✅ **已解除**，同第 1 条（A9-11）：`displayId` 现在**既参与搜索、也展示在页面上** |
| 10 | **残留不一致**：会话 / 退款 / 投诉 / 完成材料四个客服页面**仍然没有** `displayId` | 裁定只覆盖 `/staff/orders`（「不扩展业务范围」）。A9-11 已把这个残留**登记为遗留项**，交后续轮次决定是否统一。⚠️ **不是缺陷**，是**已权衡后的范围选择**，验收时不必报 |

---

## 四、验收结果

| 项 | 值 |
|---|---|
| User Result | **——（待用户本人填写）** |
| Final Result | **——（待用户本人填写）** |
| Accepted At | **——** |
| Git Commit | **——**（本批次禁止 Git 写操作；提交由用户本人完成） |
| Issues Found | **——** |
| Reviewer BLOCKER / MAJOR | **0 / 0**（复核时曾为 0 / 1，那 1 条 MAJOR 已修完复跑；4 条 MINOR 全部已修）。见 `03-delivery.md` §七 |

### 关键验收场景

| 组 | 场景 | 结果 |
|---|---|---|
| A0 | **按 `displayId`（UUID）搜得到；按旧的 `u-1001` 也仍搜得到**（A9-11 已裁定并修复） | —— |
| A | 入口 / 四套身份 / 401·404·405 | —— |
| B | 搜索（订单号 / 昵称 / 平台标识 / 商品名）+ 四个筛选 | —— |
| C | 排序 / 分页 / `total` 是过滤后的条数 | —— |
| D | 详情各区块 + 404 | —— |
| E | **超时事实在两个读路径上都被物化**（A9-4） | —— |
| F | 只读边界：页面上没有任何处置按钮 | —— |
| G | DTO 隐私（分账 / 平台净收入 / 游戏账号 / 备注 / 用户主键） | —— |
| H | 管理端 / 用户端 / 客服端四页 / 端到端回归 | —— |

> ✅ **本轮**`Status` **保持 `AWAITING_ACCEPTANCE`。**
> 验收通过后，请由**用户本人**：① 说明通过；② 完成 Git 提交；
> 之后再由用户本人（或以用户明确指令）把 `README.md` 与本文件的 `Status` 改成 `DONE` 并填入 commit。
> ⚠️ **Claude 无权自行标 `DONE`，本批次也不允许任何 Git 写操作。**
