# P0-6.1 · 04-acceptance.md

Round ID: P0-6.1
Status: AWAITING_ACCEPTANCE
**User Result: PASSED**
**Final Result: PASSED**
Accepted At: 2026-09-24
Issues Found: 无
Git Commit: （**由用户本人提交**，待填）

> ✅ **人工验收已于 2026-09-24 通过**（批次 `cmd_batch_p0-6.1_to_p0-9.md` 的统一验收）：
> 用户本人走完本文件的 A–E 五组并确认**全部通过**。日期与关键验收场景见文末
> 「验收日期 / 关键验收场景 / User Result / Final Result」。
> ⚠️ **`Status` 仍为 `AWAITING_ACCEPTANCE`，本轮不标 `DONE`**——「DONE 双门槛」的第二个条件
> （**用户本人完成 Git 提交**）尚未满足，四轮的改动至今全部躺在工作区，Claude 全程零 Git 写操作。

> ⚠️ **本文件在交付时只写 Checklist 与「待验收」，不预先填写结论。**
> `User Result` / `Final Result` 由**用户**在人工验收后给出；Claude 不得自行填 `PASSED`，
> 也不得自行把 `Status` 改成 `DONE`（`development-workflow.md` §十七「DONE 的双重门槛」）。
> 📌 **上方已填的 `PASSED` 是「记录用户给出的结论」，不是「Claude 自行判定」**——
> 用户已于 2026-09-24 明确回复「P0-6.1、P0-7、P0-8、P0-9 人工验收全部通过」。
> `Status` 仍保持 `AWAITING_ACCEPTANCE`，未被改成 `DONE`。

---

## Manual Acceptance Checklist

> 📌 **记录约定**：下面这些 `- [ ]` 条目是**交付时签发的清单**，保持原样（「追加历史、不覆盖历史」）。
> **验收结论记录在本文件末尾的「关键验收场景」表中**（组级 A–E），并如上文头部所示
> `User Result` / `Final Result` = `PASSED`（2026-09-24）。因此下方条目未逐条打勾**不代表未验收**。

> 前置：`pnpm dev` 起在 :3000（或 `pnpm build && pnpm start`）。需要时用 DEV-1 的 Mock 身份切换面板
> 在同一个窗口里换账号——本轮**不新增**任何身份切换能力。

### A. 返回入口（FIX-1）

逐个打开，确认**左上角**都有同一个入口，且**文案与样式完全一致**：

| # | 地址 | 期望 |
|---|---|---|
| A1 | `/companion`（概览） | 左上角有 `← 返回用户端` |
| A2 | `/companion/pool`（公共池） | 同上 |
| A3 | `/companion/exclusive`（专属池） | 同上 |
| A4 | `/companion/orders`（我的订单） | 同上 |
| A5 | `/companion/orders/[id]`（订单详情，从 A4 点进去） | 同上 |

逐条确认：

- [ ] **不遮挡标题**：入口单独占一行，没有压到平台名 / 导航项 / 打手昵称；窗口收窄到 375px 宽时也不重叠。
- [ ] 点它之后到达**用户端首页**（底部 TabBar 的「首页」那一格）——不是 `/mine`，不是工作台的另一页。
- [ ] **会话保持**：点完之后**仍然是登录状态**（不是游客），「我的」里能看到自己的资料。
- [ ] **再进 `/companion` 不需要重新登录**：直接回到工作台，身份与打手昵称不变。
- [ ] **没有**出现退出登录按钮、没有跳到管理员 / 客服工作台。
- [ ] ⚠️ 反例检查：如果点完变成了未登录，那说明这个入口引入了副作用——**报告，不要接受**。

> 说明：两种「进不去工作台」的提示页（还不是护航 / 资格已下架）**不在**本入口的覆盖范围内——
> 它们根本不渲染工作台顶栏，且本来就有自己的 `返回我的` → `/mine`。这是**有意保留**的，不是遗漏。

### B. 公共池顺序（FIX-2）

用 DEV-1 切到一位**有效护航**，打开 `/companion/pool`：

- [ ] 列表**顶部 = 在公共池里等得最久的一单**，**底部 = 最新进入公共池的一单**。
- [ ] 每张卡片上的「剩余时间」**不再**是自上而下的递增 / 递减关系（顺序由**进入时刻**决定，不是由到点时刻决定）。
- [ ] **刷新几次，顺序不变**（不是随机抖动，也不随请求顺序变化）。
- [ ] 现在下**一张新单**（用户端下单并支付），回到 `/companion/pool` 刷新：**新单出现在最底部**，而不是插到中间或顶部。

### C. P0-6 取消回池后的顺序（FIX-2 的**关键**一条）

1. 记下 `/companion/pool` 当前顶部那张单（它就是等了最久的）。
2. 用用户端下一个**新单**（记作 A），并观察它出现在**最底部**。
3. 切到打手，**接单 A**，然后在「我的订单」里**填原因取消 A**。
4. 回到 `/companion/pool` 刷新：

- [ ] **A 出现在最底部**（按**这一次重新进入**公共池的时刻排序），**不是**因为它创建得早而弹到最前面。
- [ ] A 原来的位置没有被任何订单挤乱：**其它订单的相对顺序与第 1 步一致**。
- [ ] ⚠️ 若 A 跳到了顶部（或中间），说明排序用了 `Order.createdAt` 而不是新的 `publicPoolEnteredAt`——**报告，不要接受**。

### D. 专属池顺序（FIX-2）

- [ ] 打开 `/companion/exclusive`（用**被指定**的那位护航身份）：顶部 = **最早进入专属池**的一单。
- [ ] 该页**只有**指定给当前打手的单（没有别人的、也没有自己下的单）——本轮**没有**改动可见性规则，只是顺序变了。

### E. 没有多出来的东西（回归）

- [ ] 用户端（`/`、`/mine`、订单列表等）**不出现**工作台顶栏，也没有多出任何「返回工作台」按钮。
- [ ] 打手工作台里**没有**退出登录按钮（本轮没加）。
- [ ] 后台 / 客服工作台的页面与登录方式**没有任何变化**。

---

## 验收日期

**2026-09-24**（本文件处于批次 `cmd_batch_p0-6.1_to_p0-9.md`，四轮由用户在 P0-9 完成后**一次性统一验收**）

## User Result

**PASSED**

## 关键验收场景（本轮）

用户本人于 2026-09-24 走完本文件的 A–E 五组验收并确认**全部通过**。覆盖的关键场景：

| 组 | 场景 | 结果 |
|---|---|---|
| A | 工作台**统一顶栏**出现 `← 返回用户端`，覆盖 `/companion` · `/companion/pool` · `/companion/exclusive` · `/companion/orders` · `/companion/orders/[id]`；点击回到用户端主入口 `/` | ✅ PASS |
| A | **会话保持**：返回后仍是同一个已登录用户（会话 Cookie / Mock 身份 / 护航资格 / 用户端资料不变）；再进 `/companion` 不需要重新登录；**没有** logout、**没有**身份切换、**没有**第二套 Companion 认证 | ✅ PASS |
| B | 公共池**顶部 = 在当前池等待最久**、**底部 = 最新进入**；刷新多次顺序稳定；运行时新下单出现在**最底部** | ✅ PASS |
| C | **P0-6 取消回池**：被接单后取消的订单按**这一次重新进入**公共池的时刻（新的 `publicPoolEnteredAt`）参与排序，出现在**靠后 / 底部**——**不是**因 `Order.createdAt` 早而弹到最前；其它订单相对顺序不乱 | ✅ PASS |
| D | 专属池顶部 = **最早进入专属池**的一单；该页可见性规则未变 | ✅ PASS |
| E | 回归：用户端不出现工作台顶栏、打手工作台里没有退出登录按钮、后台 / 客服工作台的页面与登录方式无变化 | ✅ PASS |

## Issues Found

无（本轮验收未发现新问题）

## Final Result

**PASSED**

## Git Commit

（**由用户本人提交**——Claude 不执行任何 Git 写操作；本字段由用户在提交后填写）

> ⚠️ **本轮状态仍为 `AWAITING_ACCEPTANCE`，不标 `DONE`。**
> 按 `development-workflow.md` §十七 的「DONE 双门槛」，两个条件必须**同时**满足：
> ① 用户本人说明验收通过 —— ✅ 已于 2026-09-24 满足；
> ② 用户本人完成该项目相关的 Git 提交 —— ☐ **尚未满足**。
> 因此在用户本人提交之前，本轮停在 `AWAITING_ACCEPTANCE`，由用户本人决定如何提交。
> ⚠️ `Status: AWAITING_ACCEPTANCE` 与 `Accepted At: 2026-09-24` **并不矛盾**：
> 前者是「Round 是否收口」的唯一状态字段，后者记录人工验收通过的时间。
