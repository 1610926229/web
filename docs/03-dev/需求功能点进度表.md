# 需求功能点进度表（P0 / P1 / P2 只读实现审计）

> **本文件性质：只读实现审计结果。**
> 审计日期：**2026-09-24**　审计方式：**只读**——不开发、不修改任何代码、不修改任何既有文档、零 Git 写操作。
> 审计对象：用户给出的 **122 项** P0/P1/P2 功能清单（原表 6 列：优先级 / 模块 / 功能 / 功能说明 / 建议 / 用户确认）。

---

## 一、状态口径（严格按用户给定）

| 状态 | 判定标准 |
|---|---|
| `DONE` | 代码已存在，且已有**可用完整链路**（page / API / service / data / 测试任一维度可查证端到端可用） |
| `AWAITING_ACCEPTANCE` | 已经开发完成，但**当前 Round 尚待用户验收** |
| `PARTIAL` | 存在**部分实现、Mock、占位**，或缺少完整业务闭环 |
| `NOT_IMPLEMENTED` | **明确没有实现** |

**禁止根据需求文档中「应该有」推断 `DONE`。** 本表每一行的「代码证据」列均为真实文件 / 路由 / 函数 / 测试，凡证据不足者一律写「未找到」，不猜。

### 判定规则（本表实际执行的口径）

1. **有真实代码在为该功能服务**（哪怕只是占位页、Mock 通道、只读种子）→ `PARTIAL`；**完全没有代码**（或只有枚举/类型却没有写入路径）→ `NOT_IMPLEMENTED`。
2. 「页面存在」不等于实现；**「页面上有一个数字」不等于该口径已实现**（例：管理后台概览页有数字，但全是「全量累计」，没有「今日」口径 → 今日订单数判 `NOT_IMPLEMENTED`）。
3. 结构位（状态机里的一条边、类型里的一个联合成员、常量里的一句文案）**不算实现**，除非有真实调用点。

### ⚠️ 一处重要口径说明：`AWAITING_ACCEPTANCE` 在本表中为 0

用户提交验收（2026-09-24「P0-6.1、P0-7、P0-8、P0-9 人工验收全部通过」）之后，**用户本人已完成 Git 提交**：当前 HEAD 为 `eef4e62`（`115 files changed, 19741 insertions(+), 282 deletions(-)`），其中包含 `docs/03-dev/rounds/P0-6.1 / P0-7 / P0-8 / P0-9` 四轮的全部档案，工作区 `git status` 为 **clean**。

按项目自己的「**DONE 双门槛**」（① 用户本人说明验收通过 ＋ ② 用户本人完成该项目的 Git 提交），这两个条件**现已同时满足**，因此由这四轮交付的功能点在**本审计中记为 `DONE`**——它们不满足用户对 `AWAITING_ACCEPTANCE` 的定义（「当前 Round 尚待用户验收」）。

> 📌 **文档层面遗留：已于 2026-09-24 纯文档收口完成。** 审计时四轮的 `README.md` / `04-acceptance.md` 里 `Status:` 字段**字面仍写着 `AWAITING_ACCEPTANCE`**（那是提交前写入的正确状态）；用户随后指示执行收口，现已同步为：
> - 四轮的 `README.md` 与 `04-acceptance.md` → `Status: DONE`、`Git Commit: eef4e62`；
> - `docs/03-dev/rounds/README.md` → 索引表四行 `DONE` / `eef4e62`；
> - `docs/03-dev/总需求进度表.md` → 四行 Round 及相关派生行同步为 `✅ DONE`；
> - **本文件**（`需求功能点进度表.md`）→ 同步本节。
>
> ⚠️ 该收口**只改状态与提交号**：`User Result` / `Final Result` / `Accepted At` 与全部验收结论、业务代码、测试**一律未改动**；历史档案中的中间状态按「追加历史、不覆盖历史」保留。

**受影响的功能点**（本表按 `DONE` 计，并在备注中标出轮次）：
`P0-6.1` → 等待最久订单优先；`P0-7` → 开始服务；`P0-8` → 提交完成证明 / 填写完成说明 / 客服审核完成 / 驳回完成申请 / 自动审核通过；`P0-9` → 完成后投诉 / 计算打手收入 / 收益冻结期 / 收益可结算 / 打手收益明细。

---

## 二、P0 · 57 项

| 优先级 | 模块 | 功能 | 状态 | 代码证据 | 备注 |
|---|---|---|---|---|---|
| P0 | 首页 | 商品展示 | `DONE` | `app/(mobile)/(tabs)/page.tsx` → `lib/services/home.ts getHomeData()` → `lib/data/mockSource.ts` → `listPublicProducts()` + `ProductSection` 渲染 | 只展示上架/未移除/挂可见类目的商品；后台下架即时从首页消失 |
| P0 | 首页 | 商品分类 | `DONE` | `app/(mobile)/(tabs)/category/page.tsx` → `lib/services/catalog.ts queryProducts({gameId,categoryId})` → `mockSource.queryProducts` 精确过滤；`/api/catalog/products` 同源 | 分类口径在服务端/数据层过滤，非前端；种子 25 商品分属 6 类目，真能筛出不同商品 |
| P0 | 首页 | 公告 | `DONE` | `AnnouncementCarousel` ← `getHomeData().announcements` ← `mockContentRepository.listAnnouncementRecords()` + `lib/constants/homeContent.ts selectPublicAnnouncements()` | 与后台公告**同一份 store**；上下架（`enabled`/`removedAt`）真实影响可见性。仅图片滚动、**不响应点击** |
| P0 | 首页 | 活动入口 | `PARTIAL` | 首页活动图：`lib/constants/homeContent.ts selectActivityImageUrl()` ← 后台 `lib/services/adminBanners.ts`（真实配置链路）；但 `/activities` = `app/(mobile)/activities/page.tsx` **只渲染 `PlaceholderPage`** | 首页代码注释自陈「活动展示图：内容由管理端配置，**当前为占位图**」；快捷入口「点单权益」指向 `/placeholder`。入口在，落地页是占位 |
| P0 | 商品 | 商品详情 | `DONE` | `app/(mobile)/product/[id]/page.tsx` → `lib/services/catalog.ts getProductDetail()` → `toProductDetail()`；`ProductPurchasePanel` 渲染 | 下架商品可直链浏览但显示「已下架」且不能购买；软删除商品 404 |
| P0 | 商品 | 商品规格 | `DONE` | `lib/types/product.ts ProductSpecRecord{id,name,price,sortOrder,enabled,removedAt}` + `listEffectiveSpecs()`；`catalogSeed.ts` 覆盖局数（「带飞一局」）/小时数（「教学陪玩 2 小时」）/套餐（「全天包时段」） | ⚠️ 规格是**自由文本 name + 价格**，没有「局数/小时数/套餐」的**结构化字段** |
| P0 | 商品 | 增值服务 | `DONE` | `catalogSeed.ts addonSeed`（加急处理 ¥15 / 全程语音 ¥10 / 掉段保险 ¥20）→ `lib/services/checkout.ts resolveSelection()`：`addonsAmount = addons.reduce(...)`、`totalAmount = itemsAmount + addonsAmount` → 进 `lib/constants/orderAmount.ts` 原价与**分账基数** | 真参与金额；按单计费不随数量、重复 id 去重。⚠️ 增值服务是**只读种子目录，后台不可编辑** |
| P0 | 商品 | 商品发布 | `DONE` | `POST /api/admin/products` → `lib/services/adminProducts.ts createAdminProduct()` → `lib/data/adminCatalogTransaction.ts createProduct()` → `mockCatalogRepository.createProductRecord()` **真实写入 Map**；页面 `products/new` → `AdminProductCreateForm` | 测试 `tests/adminProducts.test.mjs`「新建商品：一次写入商品与全部规格」「HTTP 全链路」 |
| P0 | 商品 | 商品编辑 | `DONE` | `PATCH /api/admin/products/[id]` → `updateAdminProduct()` → `updateProduct()` → `mockCatalogRepository.applyProductPatch()`（**specs 整份替换**）；表单 `AdminProductForm` + `AdminSpecEditor` | 可改价/介绍/规格/分账比例/上下架态；`monthlySales`/`id`/时间字段服务端无读取位置，伪造无效 |
| P0 | 商品 | 商品上下架 | `DONE` | `POST /api/admin/products/[id]/publish|unpublish` → `setAdminProductStatus()` → `setProductStatus()` → `applyProductStatus()`（窄写入，只改 status+updatedAt） | 用户端 `lib/constants/catalog.ts isProductPubliclyListed()` 过滤；且**服务端拒绝结算**（「商品已下架，无法支付」），不是前端置灰。测试 `product-visibility.test.mjs` |
| P0 | 用户 | 用户登录 | `DONE` | `app/api/auth/mock-login/route.ts` + `lib/auth/RequireAuth.tsx` / `LoginGate.tsx` + `(protected)/layout.tsx` 统一包裹 | 未登录进入下单/订单页被拦。为 **Mock 登录**，非真实凭证 |
| P0 | 用户 | 微信内登录 | `PARTIAL` | 现有 Mock 登录链路 + `lib/auth/AuthAdapter.ts` 注释「未来接入公众号网页授权」；**全仓无** `openid/jscode2session/appid/appsecret/公众号/OAuth` 任何实现 | 进度表：微信真实登录—OAuth ⏳ `PLANNED`（需认证服务号）。当前身份 = Mock 用户列表选择 |
| P0 | 下单 | 创建订单 | `DONE` | `app/(mobile)/checkout/page.tsx` → `createPaymentRequest()`（`POST /api/orders/pay`，幂等键 `userId+idempotencyKey`，**服务端重算金额**）→ 支付成功 `confirmPaymentRequest()` → `buildOrderFromRequest()` 建单 | URL 只带 productId/specId，不带价格。试算走 `POST /api/orders/preview` |
| P0 | 下单 | 选择指定打手 | `DONE` | `components/checkout/CompanionSheet.tsx` → `CheckoutSelection.companionId` → `checkout.ts resolveCompanion()` 校验 → `createDispatchForOrder({exclusiveCompanionId})` **进专属池** | 「指定」只写进 `Dispatch.exclusiveCompanionId`；`Order.actualCompanionId` 在有人接单时才写 |
| P0 | 下单 | 不指定打手 | `DONE` | 同上，`companionId=null` → `exclusiveCompanionId: null` → **进公共池** | 与上一项同一函数两条分支 |
| P0 | 支付 | 微信支付 | `PARTIAL` | Mock 支付通道：`app/api/payments/mock-confirm/route.ts`（`ENABLE_MOCK_PAYMENT` 控制，success/failure/cancel）+ `components/payment/MockPaymentActions.tsx` + `app/(mobile)/pay/result/page.tsx`；**全仓无** `wxpay/jsapi/unifiedorder/mch_id/prepay` | `lib/config/env.ts` 注释「本项目没有任何真实微信支付凭据」。进度表：真实微信支付放**生产阶段** |
| P0 | 订单 | 我的订单 | `DONE` | `app/(mobile)/(tabs)/(protected)/orders/page.tsx` → `lib/services/orders.ts queryOrdersForUser()` → `OrderList`；切 Tab/搜索/加载更多走 `/api/orders` | 归属由服务端按会话用户过滤 |
| P0 | 订单 | 订单详情 | `DONE` | `app/(mobile)/orders/[id]/page.tsx` → `getOrderDetailForUser()` | 展示商品/规格/单价/数量/增值服务/金额域/游戏/大区/游戏ID/备注/打手/派单进度/时间轴/售后入口；**他人订单返回「订单不存在」** |
| P0 | 订单 | 按订单状态筛选 | `DONE` | `ORDER_TABS` → `status` 参数 → `lib/constants/orders.ts parseOrderListQuery()` → `lib/data/mockPaymentRepository.ts queryOrders()`（`.filter(order => status===null || order.status===status)`） | **数据层按 `status` 过滤**，非前端过滤；非法 status → 400 |
| P0 | 订单 | 搜索订单号 | `DONE` | `components/orders/OrderList.tsx` 搜索框 → `keyword` 参数 → `mockPaymentRepository.queryOrders()` → `matchesOrderKeyword(order.orderNo, keyword)` | 部分匹配、忽略大小写、上限 32 字符、空串不过滤 |
| P0 | 订单 | 订单状态变化 | `DONE` | `lib/constants/orders.ts`：`ORDER_STATUSES = ["paid","accepted","serving","completed","refunded"]` + `ORDER_TRANSITIONS`；写入器 `applyOrderAccepted/Serving/Completion/Refund/AcceptanceReleased`（`mockPaymentRepository.ts`）由事务层 Guard 调用 | 需求「待接单→已接单→服务中→完成」对应 `paid→accepted→serving→completed`。状态表只判结构许可，业务 Guard 另在事务层 |
| P0 | 通知 | 打手接单通知 | `DONE` | `lib/data/companionDispatchTransaction.ts acceptDispatch()`（构造 + `appendNotification`），收件人 `order.userId`；文案 `lib/constants/dispatch.ts DISPATCH_NOTIFICATION_ACCEPTED` | 用户端读取：`app/(mobile)/(tabs)/(protected)/service/page.tsx`「系统通知」→ `queryNotificationsForUser`；replay 重复提交**不重复写** |
| P0 | 通知 | 打手取消通知 | `DONE` | `lib/data/companionOrderTransaction.ts cancelAcceptedOrder()` → `writeAcceptanceRelease()` → `appendNotification`；文案 `DISPATCH_NOTIFICATION_ACCEPTANCE_RELEASED` | 注释明确「发给**下单用户**，不是发给打手」，文案不写「违规/已重新派单」 |
| P0 | 通知 | 重新接单通知 | `DONE` | 取消回池后**新打手接单走 `acceptDispatch` 全量路径**（仅「已 accepted 且 `acceptedByCompanionId===自己`」才 replay 跳过），再次写 `DISPATCH_NOTIFICATION_ACCEPTED` 给老板 | ⚠️ **没有独立的「重新接单」类型或文案**，老板收到的与首次接单同文案。若产品要求区分口径，需另立类型 |
| P0 | 打手 | 打手申请入驻 | `DONE` | `app/(mobile)/join/page.tsx` + `join/status/page.tsx` → `POST /api/companion-applications` → `createCompanionApplicationForUser()`；查询 `/api/me/companion-application`；撤销 `[id]/withdraw` | 同一人最多一条申请；只有「待查看」可撤销 |
| P0 | 打手 | 管理员审核打手 | `DONE` | `app/admin/(console)/applications/(list)` + `[id]`；`GET /api/admin/companion-applications`、`[id]/start-review|approve|reject` | `approve` 是**原子四笔**（状态 approved + 发资格 + 建/复用护航资料 + 审计，见 `lib/data/adminCompanionTransaction.ts`）；拒绝意见必填；全部先 `requireAdmin()` |
| P0 | 打手 | 打手工作台 | `DONE` | `app/companion/(console)/layout.tsx` + `lib/services/companionAccess.ts resolveCompanionAccess()`；接口守卫 `lib/api/companionRoute.ts requireCompanion()` | 8 个页面：概览 / pool / exclusive / orders / orders/[id] / earnings（+ not-found、loading）；`(console)` 不产生 URL 段 |
| P0 | 打手 | 公共订单池 | `DONE` | `app/companion/(console)/pool/page.tsx` → `lib/services/companionDispatch.ts listCompanionPools()` 取 `pools.public`；`GET /api/companion/dispatches` | 只读列表接口（`dispatches/route.ts` 只有 `GET`） |
| P0 | 打手 | 专属订单池 | `DONE` | `app/companion/(console)/exclusive/page.tsx` → `listCompanionPools()` 取 `pools.exclusive` | 只显示指定给当前打手的单 |
| P0 | 打手 | 等待最久订单优先 | `DONE` | `lib/services/companionDispatch.ts compareByWaitingSince()`（第 254 行）：`waitingSince` **ASC**、并列按 `dispatchId` ASC；`currentPoolEnteredAt()` = 专属池取 `exclusiveEnteredAt`、公共池取 `publicPoolEnteredAt`；第 157–158 行两池分别 sort，排序键**不进 DTO** | **P0-6.1 交付**（验收通过 + 已随 `eef4e62` 提交）。测试 `tests/companionPoolOrder.test.mjs` 11.1 / 11.3（取消回池用**新**时刻，排到底部）/ 11.4 / 11.6；旧 `compareByDeadline` 已删除 |
| P0 | 打手 | 接单 | `DONE` | `POST /api/companion/dispatches/[id]/accept` → `acceptDispatchForCompanion()` → `lib/data/companionDispatchTransaction.ts acceptDispatch()`（原子：派单 accepted + 订单 accepted + 通知） | 判定：已被接/已到点/专属指定/接单资格/**禁止自接单** |
| P0 | 打手 | 我的订单 | `DONE` | `app/companion/(console)/orders/page.tsx` → `listCompanionOrders()`（归属 `Order.actualCompanionId`）；详情页服务端**重校验归属**，否则 `notFound()` | 别人的单改地址得到 404，不区分「不存在」与「不是你的」 |
| P0 | 打手 | 取消已接订单 | `DONE` | `POST /api/companion/orders/[id]/cancel` → `lib/services/companionOrders.ts cancelCompanionOrder()`：`readTrimmedString(body,"reason")` **非空必填** + 幂等键校验 → `lib/data/companionOrderTransaction.ts cancelAcceptedOrder()` | **P0-6 交付**（已 `DONE` 收口，提交 `53481ea`）。仅当前 `actualCompanionId`、仅 `accepted` 可取消；注释明确不发明 5~50 字规则（字数规则只用于完成说明） |
| P0 | 打手 | 取消后重新派单 | `DONE` | `companionOrderTransaction.ts writeAcceptanceRelease()`：同一原子区段写四件事 —— `appendCompanionRelease`（退出历史）+ `applyOrderAcceptanceReleased`（清履约绑定）+ `applyDispatchToPublic`（**重写 `publicPoolEnteredAt` = 取消时刻并重冻超时**）+ `appendNotification` | **P0-6 交付**（已 `DONE`）。重写进入时刻正是「取消回池后排在底部」的实现依据 |
| P0 | 打手 | 开始服务 | `DONE` | `POST /api/companion/orders/[id]/start`（无请求体）→ `startCompanionOrder()` → `lib/data/companionOrderTransaction.ts` → `applyOrderServing()` | **P0-7 交付**（验收通过 + 已随 `eef4e62` 提交）。幂等判据为**状态本身**；刻意**不发通知**（需求未冻结该通知） |
| P0 | 打手 | 提交完成证明 | `DONE` | `POST /api/companion/orders/[id]/completion` → `lib/services/companionCompletions.ts submitCompanionCompletion()` → `lib/data/completionTransaction.ts submitCompletion()`（`evidence: SupportEvidence[]` 写入）；UI `CompanionCompletionPanel` + `EvidencePicker`（图片/视频，≤6 个，可 0 个） | **P0-8 交付**（验收通过 + 已提交）。⚠️ Mock 阶段凭证 URL 为占位图 `/mock/evidence-placeholder.svg`，**真实文件存储未接入** |
| P0 | 打手 | 填写完成说明 | `DONE` | `lib/constants/completions.ts`：`COMPLETION_SUMMARY_MIN_LENGTH=5` / `MAX_LENGTH=50` / `normalizeCompletionSummary()`（`countCharacters`，Emoji 算 1 字）；服务端 `companionCompletions.ts` 调用，前端复用并显示 `count/50` | **P0-8 交付**。按**字符数**非字节；与「取消原因无字数要求」是两条刻意的不同规则 |
| P0 | 完成审核 | 客服审核完成 | `DONE` | `app/staff/(console)/completions/(list)` + `[id]`；`POST /api/staff/completions/[id]/approve` → `approveStaffCompletion()` → `approveCompletionTransaction`（其中 `canTransitionOrder(order.status,"completed")` 推订单 serving→completed） | **P0-8 交付**。审核人/审核时间保留 |
| P0 | 完成审核 | 驳回完成申请 | `DONE` | `POST /api/staff/completions/[id]/reject` → `rejectStaffCompletion()`（**驳回原因必填**）→ `rejectCompletionTransaction` | **P0-8 交付**。驳回后**订单保持 `serving`**、不进金额、不产生收益；打手可重新提交且**不覆盖旧审核历史** |
| P0 | 完成审核 | 自动审核通过 | `DONE` | `sweepCompletionAutoApprovals(at)`（惰性物化）+ `autoApprovalDeadlineAt` 快照 + `reviewSource="system"`；阻塞判据 `readOrderBlockingFacts()` + `isCompletionAutoApprovalBlocked()` | **P0-8 交付**。⚠️ **无真实调度器**——由四条读路径（用户订单 / 管理端订单 / 客服完成材料 / 打手订单，列表也算）触发；同一函数将来由 Scheduler 调用。已 `resolved/closed` 的投诉**不阻塞**（`02-decisions.md` D4） |
| P0 | 售后 | 未接单退款 | `DONE` | `lib/data/companionDispatchTransaction.ts sweepExpiredDispatches()` → `applyOrderRefund(orderId, timedOutAt, order.actualPaidAmount)`（**自动全额退款**）+ 关派单 + 发通知；配置 `PUBLIC_POOL_TIMEOUT_DEFAULT_MINUTES=60`（1~1440） | 语义链：**专属池 10 分钟（固定）→ 转公共池 → 公共池超时才退款**。自动退款**不创建售后单、不产生退款申请**。测试 `tests/dispatch.test.mjs` 自动退款 15/16/17/18。⚠️ 同样靠**读取触发**，无调度器 |
| P0 | 售后 | 已接单未服务退款 | `PARTIAL` | 用户侧：`app/(mobile)/orders/[id]/refund/page.tsx` → `POST /api/orders/[id]/refunds` → `lib/services/refunds.ts createRefundForOrder()`（`amount = order.totalAmount` 整单、`status="pending"`，**只写申请不动订单**）；`REFUNDABLE_ORDER_STATUSES` 含 `paid/accepted/serving/completed` | ⚠️ **只有「申请」闭环**：真正退款必须管理员 `approve`。2026-09-23 新规则要求 `paid/accepted` **直接全额退款免审批**，进度表标 🟠 `NEEDS_FIX`，**尚未实现**。金额不可编辑（`parseRefundInput` 白名单无 amount） |
| P0 | 售后 | 服务中售后 | `DONE` | `serving` 在可退状态内，用户可提交退款申请；客服端 `lib/services/staffRefunds.ts startReviewStaffRefund()/rejectStaffRefund()` + 投诉处置动作；管理员端 `adminRefunds.ts` | 客服**刻意没有 approve**（`staffRefundAllowedActions` 无 `canApprove`）——资金最终划拨只在管理员侧，这是 P0-5.5 的既定口径，非缺口 |
| P0 | 售后 | 完成后投诉 | `DONE` | `app/(mobile)/complaints/new/page.tsx` → `POST /api/complaints` → `createComplaintForUser()`；窗口判定 `isComplaintWindowClosed(order, now)` → 到点抛 400 `COMPLAINT_WINDOW_CLOSED_MESSAGE` | **P0-9 交付**（验收通过 + 已提交）。`complaintDeadlineAt = completedAt + complaintWindowMinutesSnapshot`（完成时冻结、**不追溯**；默认 1440 分钟，60~10080）。投诉**不自动退款、不改订单状态** |
| P0 | 售后 | 管理员退款处理 | `PARTIAL` | `app/admin/(console)/refunds/**` + `POST /api/admin/refunds/[id]/start-review|approve|reject` → `lib/data/adminRefundTransaction.ts approveRefund()`（同段无 await：approved + 审核人 + `applyOrderRefund(existing.orderId, …, order.actualPaidAmount)` + 审计） | **审核是真，但「由管理员决定最终退款金额」不成立**：金额取自订单实付快照，接口请求体**无任何接收金额的字段**；且**全库无部分退款**（`grep partial` 零命中，`Order.refundedAmount` 注释标明部分退款属后续批次）。测试「退款金额不可篡改」「金额真值源是订单不是退款申请」 |
| P0 | 客服 | 查看订单 | `NOT_IMPLEMENTED` | **客服端无 `/staff/orders` 页面，也无 `/api/staff/orders` 接口**——`tests/staff.test.mjs`「客服接口清单固定」逐条列出全部 20 个 route（认证三件 + 会话四件 + 退款四件 + 投诉五件 + 完成材料四件），**没有 orders**；`app/staff/(console)/layout.tsx` 注释原文「客服**没有订单全量查询**（只看得见有会话的订单）」 | 客服只能从**四条业务路径**（会话 / 投诉 / 退款 / 完成材料）间接带出**裁剪过的**只读订单摘要：orderNo、productTitle、orderStatus、totalAmount、releaseHistory 等。**永远读不到**游戏 ID、订单备注、`userId`、OpenID、支付凭据。需求侧「客服查询订单和用户情况」= **必须**，属 P0 缺口 |
| P0 | 客服 | 处理售后 | `DONE` | 投诉：`POST /api/staff/complaints/[id]/start-processing|resolve|close` → `staffComplaints.ts`，动作由 `canTransitionComplaint` **推导**（无手写 if）；退款：`start-review` / `reject`（必填原因）；完成材料：`approve` / `reject` | 全部先 `requireStaff()`。投诉状态机 `pending→[processing,closed]`、`processing→[resolved,closed]`，`resolved/closed` 为终态 |
| P0 | 客服 | 更换打手 | `NOT_IMPLEMENTED` | `lib/constants/orders.ts` 有 `serving: ["paid",…]` 边，但第 86–92 行注释原文「是封禁回池 / 客服换人的**结构位置，本批次不实现、也不提供任何 API 或按钮**」；`canTransitionOrder` 全仓仅 **2 个调用点**（`companionOrderTransaction.ts` →serving、`completionTransaction.ts` →completed），**无任何 `→"paid"` 调用**；`CompanionReleaseSource` 的 `staff_reassign` 只有类型 + 文案（`lib/constants/dispatch.ts`），`grep staff_reassign` 于 `app/` **零命中** | 进度表「客服换人 / 重新派单」⏳ `PLANNED`。⚠️ 需求文档《用户权限表》声称客服「可直接执行更换打手」，那是**权限/需求声明，不是代码事实**。已实现的只有**防御**（换人后旧完成材料失效），不等于换人能发生。需求侧 = **必须**，属 P0 缺口 |
| P0 | 收益 | 计算打手收入 | `DONE` | `lib/data/earningTransaction.ts settleOrderCompletion()`（唯一结算入口）：订单 `serving→completed` 时同步「完成订单 + 冻投诉窗口 + 生成 `frozen` Earning」；`incomeAmount = settled.companionBaseIncome`（**搬快照，不重算**）；`companionId = actualCompanionId`（**实际履约者**，非用户指定者） | **P0-9 交付**。两条完成来源（客服人工 `approveCompletion` / System 到期）**都只调用它**。起始状态守卫：非 `serving` **一个字节都不写** |
| P0 | 收益 | 收益冻结期 | `DONE` | `lib/types/earning.ts EarningStatus = "frozen" \| "available" \| "withdrawn" \| "reversed"`；新收益一律 `status:"frozen"`、`frozenAt = 订单 completedAt`；`availableAt = Order.complaintDeadlineAt` | **P0-9 交付**。`complaintWindowMinutesSnapshot` 在订单进入 `completed` 时写入且**不刷新、不追溯**（EX-CONFIG-06） |
| P0 | 收益 | 收益可结算 | `DONE` | `lib/data/earningTransaction.ts sweepMaturedEarnings(at)`（**同步函数**，无 await）→ 对 `frozen` 且 `availableAt <= at` 且无阻塞者 `applyEarningRelease` → `available`；判据 `lib/constants/earnings.ts isEarningMatured()`；阻塞判据与 P0-8 自动通过**复用同一对函数** | **P0-9 交付**。幂等：已是 `available` 返回 `changed:false` 且**不刷新 `availableAt`**。⚠️ **无真调度器**，唯一调用点 `lib/services/companionEarnings.ts materializeEarningReleases()` 挂在**读取路径**上 |
| P0 | 管理员 | 订单管理 | `DONE` | `app/admin/(console)/orders/(list)` + `[id]`；`GET /api/admin/orders`（`requireAdmin()`，严格模式非法枚举/日期 → 400）；筛选 `status/game/from/to/keyword`；详情拼四份售后摘要 + 时间轴 + 指定/实际护航 + **履约退出历史** | 详情页**整页只读、无任何按钮**。筛选在 service 层**真实生效**（测试「组合筛选：状态、游戏、时间范围与关键词同时生效」） |
| P0 | 管理员 | 商品管理 | `DONE` | 发布 / 编辑 / 上下架 / **软删除**（`POST [id]/remove` → `applyProductRemoval()` 只写 `removedAt`，不物理删）/**分类管理**（`adminCategories.ts` 8 个函数 + `adminCatalogTransaction` 重名与「有未移除商品不能删」在原子区段判定） | 测试 `adminProducts.test.mjs` / `adminCategories.test.mjs` / `product-visibility.test.mjs`。⚠️ **游戏管理未实现**（`catalogSeed.ts` 注释「游戏不由后台管理」），但原表未列此项 |
| P0 | 管理员 | 打手管理 | `DONE` | `app/admin/(console)/companions/(list)` + `[id]`；`GET/PATCH /api/admin/companions/[id]` + `enable/disable/pause/resume/remove` → `lib/services/adminCompanions.ts`（`setAdminCompanionFlags()` / `removeAdminCompanion()`） | 审核侧见「管理员审核打手」。禁用/移除真实影响打手端 `requireCompanion()` 资格判定 |
| P0 | 管理员 | 客服管理 | `DONE` | `app/admin/(console)/customer-service/(list)` + `new` + `[id]`；`POST /api/admin/staff` → `createAdminStaff()` → `lib/data/adminStaffTransaction.ts createStaffAccount()` → `mockStaffRepository.createStaffRecord()` **真实写入 store**；`[id]/enable|disable|remove` | 真实生效：`requireStaff` → `getStaffSessionState()` → `canStaffSignIn(account)`（`enabled && removedAt===null && role` 可进）；停用/移除后旧 Cookie **下一次请求即 403**。⚠️ **无密码字段**，登录为 `/staff/login` 的 Mock 账号选择；新建角色写死 `customer_service` |
| P0 | 管理员 | 公告管理 | `DONE` | `app/admin/(console)/content/announcements/page.tsx` + `components/admin/AdminAnnouncementConsole.tsx`（新建/编辑为**内联表单**）；`GET/POST /api/admin/content/announcements` + `[id]` + `[id]/enable|disable|remove` → `lib/services/adminAnnouncements.ts` → `lib/data/adminContentTransaction.ts`（写入 + 审计同原子区段） | **与用户端首页读的是同一份 `contentRepository`**，上下架真实影响可见性。⚠️ 无 `[id]/new` 独立页面目录；**无生效时间/定时上下架、无置顶字段**（只有 `sortOrder`） |
| P0 | 管理员 | 平台参数设置 | `DONE` | `app/admin/(console)/platform-config/page.tsx` → `AdminPlatformConfigConsole` → `lib/services/adminHttp.ts fetchAdminPlatformConfig()/saveAdminPlatformConfig()` → `GET/PATCH /api/admin/platform-config`（均先 `requireAdmin()`）→ `lib/services/adminPlatformConfig.ts` | **三项齐全**：接单超时 `publicPoolTimeoutMinutes`（1~1440，默认 60）、自动审核时间 `completionAutoApprovalMinutes`（1~1440，默认 10）、投诉期限 `complaintWindowMinutes`（**60~10080**，默认 1440）。`PATCHABLE_FIELDS` 白名单 + 快照语义（改配置**不追溯**已发生的事） |

---

## 三、P1 · 38 项

| 优先级 | 模块 | 功能 | 状态 | 代码证据 | 备注 |
|---|---|---|---|---|---|
| P1 | 订单 | 按时间查询历史订单 | `NOT_IMPLEMENTED` | `lib/constants/orders.ts OrderListQuery` 只有 `status / keyword / page / pageSize` 四个字段，**无任何时间参数**；`components/orders/OrderList.tsx` 第 22 行 `FILTER_HINT_TEXT = "更多筛选功能待补充"` | 注释明确「不自行发明日期/金额/商品等筛选规则」。当前只有「状态 Tab + 订单号关键字」 |
| P1 | 订单 | 按游戏查询 | `NOT_IMPLEMENTED` | 同上：`OrderListQuery` 无 `gameId`，列表无游戏筛选 UI | — |
| P1 | 订单 | 按商品类型查询 | `NOT_IMPLEMENTED` | 同上：无 `categoryId` / `productType` 参数 | — |
| P1 | 订单 | 按打手查询 | `NOT_IMPLEMENTED` | 同上：无 `companionId` 参数 | — |
| P1 | 订单 | 按金额查询 | `NOT_IMPLEMENTED` | 同上：无金额区间参数 | — |
| P1 | 订单 | 再来一单 | `NOT_IMPLEMENTED` | 全仓 `grep 再来一单\|再次购买\|reorder\|rebuy` **无任何页面/组件/服务命中**；唯一命中是 `lib/constants/catalog.ts:152` 的一句将来注释 | 订单详情页与 `OrderCard` 均无再次购买入口 |
| P1 | 老板数据 | 累计订单数 | `NOT_IMPLEMENTED` | `app/(mobile)/(tabs)/(protected)/mine/page.tsx` 只渲染 `ProfileCard` + `LevelSummaryPanel` + `MineMenu`，**无订单数** | 数据层有 `countEffectiveOrders()`（`lib/constants/levels.ts:67`）但**没有页面/接口外发** |
| P1 | 老板数据 | 累计消费金额 | `PARTIAL` | `mine` 页 `loadLevelSummary` → `lib/services/levels.ts getConsumptionLevelForUser()`（`sumEffectiveSpend(orders)` 算累计有效消费，仅 `completed`）→ `components/mine/LevelSummaryPanel.tsx` → `LevelSummaryCard`（当前等级 + 累计金额 + 进度） | 数据真实（订单实付快照）。⚠️ 形态是**「消费等级」而非老板数据面板**：不含订单数、近 30 天、常玩游戏、常用打手 |
| P1 | 老板数据 | 最近30天消费 | `NOT_IMPLEMENTED` | `grep 最近30天\|近30天` 于 `components/lib` **无命中**（仅 docs/测试） | — |
| P1 | 老板数据 | 常玩游戏 | `NOT_IMPLEMENTED` | `grep 常玩游戏\|常玩` 无业务命中 | — |
| P1 | 老板数据 | 常用打手 | `NOT_IMPLEMENTED` | `grep 常用打手\|常用陪玩\|最近使用打手` 无命中 | — |
| P1 | 打手数据 | 今日接单数 | `NOT_IMPLEMENTED` | `app/companion/(console)/page.tsx` 顶部注释原文「**只有静态内容**……这一页**不读任何数据**：不读会话、不查资格、不查仓储」，实际只渲染两句文案常量 | 打手端真实数据页只有 `/companion/orders`、`/companion/earnings`（且收益页只有两个合计） |
| P1 | 打手数据 | 本月接单数 | `NOT_IMPLEMENTED` | 同上 | — |
| P1 | 打手数据 | 今日收入 | `NOT_IMPLEMENTED` | `app/companion/(console)/earnings/page.tsx` 只显示 `summary.frozenAmount`（冻结中）与 `summary.availableAmount`（可提现）两个**合计**，**无今日/本月口径** | — |
| P1 | 打手数据 | 本月收入 | `NOT_IMPLEMENTED` | 同上 | — |
| P1 | 打手数据 | 完成率 | `NOT_IMPLEMENTED` | 全仓 `grep 完成率\|接单率\|取消率` **零命中** | — |
| P1 | 打手数据 | 取消率 | `NOT_IMPLEMENTED` | 同上 | — |
| P1 | 排行榜 | 打手接单排行榜 | `NOT_IMPLEMENTED` | `lib/constants/rankings.ts` 只有 `buildConsumptionRanking`（**用户消费榜**），**无打手维度**；`grep 接单排行\|打手排行` 无命中 | — |
| P1 | 排行榜 | 打手完成订单排行榜 | `NOT_IMPLEMENTED` | 同上 | — |
| P1 | 排行榜 | 打手收入排行榜 | `NOT_IMPLEMENTED` | 同上 | — |
| P1 | 排行榜 | 用户端排行榜 | `NOT_IMPLEMENTED` | `app/(mobile)/rank/page.tsx` + `lib/services/rankings.ts getConsumptionRanking()` 实现的是**用户消费额榜**（按订单实付聚合**用户**），标题 `RANKING_PAGE_TITLE="消费排行榜"` | ⚠️ 现有榜单与需求「老板看到**热门/优秀打手**」**不是同一件事**。打手列表 `/companions` 按 `sortOrder`（手工排位）排序，**无热门/优秀打手榜** |
| P1 | 评价 | 订单评价 | `DONE` | `app/(mobile)/reviews/new/[orderId]/page.tsx` → `POST /api/orders/[id]/reviews` → `lib/services/reviews.ts createReviewForOrder()`（幂等 + 归属校验 + 状态校验）→ `lib/data/reviewRepository.ts` → store；读回：`/reviews`（`queryReviewsForUser`）、订单详情 `reviewSummary`、管理端订单详情 | 测试覆盖幂等与归属。⚠️ **评价不回显到打手端/商品端**——打手详情页的评分来自 `lib/mocks/fixtures/seed.ts` 里**写死**的 `reviews/reviewCount`，与 reviewRepository **不连通**；进度表「评价（一单一次、审核）」为 ⏸ `PAUSED` |
| P1 | 评价 | 星级评分 | `DONE` | `lib/services/reviews.ts parseReviewInput()`：`isReviewRating` 校验 **1~5 星**（`lib/constants/reviews.ts`） | 客户端伪造越界评分被拒 |
| P1 | 评价 | 文字评价 | `DONE` | 同上：`normalizeReviewContent(readTrimmedString(body,"content"))` | 与星级同一写入路径 |
| P1 | 打手 | 收藏打手 | `NOT_IMPLEMENTED` | `app/(mobile)/favorites/page.tsx` 标题为**「商品收藏」**；`lib/services/favorites.ts` 全部以 **`productId`** 为业务主键（`addFavoriteForUser` / `removeFavoriteForUser` / `queryFavoritesForUser`）；`lib/constants/mine.ts` 入口 label 也是「商品收藏」 | 收藏**基础设施已存在**（幂等、真实 store），但**没有打手收藏的字段/页面** |
| P1 | 打手 | 最近使用打手 | `NOT_IMPLEMENTED` | `grep 最近使用\|recentCompanion\|常用陪玩` 无命中 | — |
| P1 | 打手 | 搜索打手 | `DONE` | `app/(mobile)/companions/(list)/page.tsx` → `queryCompanionPage()` → `lib/constants/companions.ts buildCompanionListQuery()`（含 `keyword`）→ `companionMatchesKeyword()`（按昵称 / 自我介绍 / 服务标签匹配）；UI `CompanionBrowser` | — |
| P1 | 收益 | 打手收益明细 | `DONE` | `app/companion/(console)/earnings/page.tsx` + `components/companion/CompanionEarningList.tsx`；`GET /api/companion/earnings`（仅 GET，`requireCompanion()`，`companionId` **只来自会话**） | **P0-9 交付**。每卡显示：**订单号**、状态中文名、**本单收益金额**、状态说明、**冻结时间**、**预计解冻**；整卡链到订单详情，**无提现按钮**。DTO 恰 **8 个字段**，`tests/earningsHttp.test.mjs` 用 `FORBIDDEN_KEYS` 钉死不含 `clubNetIncome/companionRateSnapshot/totalAmount` 等 |
| P1 | 收益 | 提现申请 | `NOT_IMPLEMENTED` | **零实现**：全仓 `grep withdraw\|提现` 的唯一命中是护航入驻申请的**「撤销」**（`app/api/companion-applications/[id]/withdraw`），与金钱无关 | 仅有 `EarningStatus` 的 `"withdrawn"` 枚举 + `EARNING_STATUS_LABELS` 的「已提现」文案（类型骨架，**无页面/接口/写入路径**）。多处代码注释明示「本阶段不做提现」；进度表明列「提现」为**非核心 P0 主线项** |
| P1 | 收益 | 提现审核 | `NOT_IMPLEMENTED` | 全仓无任何提现审批代码（`components/admin/` 下收益相关只有 `AdminPlatformConfigConsole.tsx`） | 同上 |
| P1 | 管理 | 高级订单筛选 | `PARTIAL` | `lib/constants/adminOrders.ts AdminOrderListQuery = {status, keyword, game, from, to, page, pageSize}`；`status/game/from/to` 在 `lib/data/mockPaymentRepository.ts queryOrdersForAdmin()` 真实过滤，`keyword` 在 `lib/services/adminOrders.ts` 跨实体匹配（订单号 / 商品名 / 用户昵称 / 平台 ID） | 测试「组合筛选：状态、游戏、时间范围与关键词同时生效」（`tests/adminOrders.test.mjs`）。⚠️ **无「用户」「商品」「打手」独立筛选项**：用户与商品只能靠 keyword 模糊命中，**打手完全无法筛选**（`AdminOrderQueryFilter` 无 companion 字段） |
| P1 | 管理 | 今日订单数 | `NOT_IMPLEMENTED` | `app/admin/(console)/(overview)/page.tsx` 只渲染 `getAdminOverview()` 的 **7 个指标**；`lib/services/adminConsole.ts` 的 `metrics` 只含 `applications.*`（4 个）与 `companions.*`（3 个），**全文从不触碰订单仓储** | `lib/constants/admin.ts ADMIN_OVERVIEW_NOTICE` 明确「本页只汇总入驻申请与护航规模，订单、退款与投诉在各自的页面里查看」 |
| P1 | 管理 | 今日成交额 | `NOT_IMPLEMENTED` | 同上：7 个指标全是**计数**，**无任何金额字段**，且**无日期过滤（全是全量累计）** | 订单列表每行显示单笔 `totalAmount`，但无聚合/流水汇总，更无「今日」口径 |
| P1 | 管理 | 今日退款金额 | `NOT_IMPLEMENTED` | 概览页与 `adminConsole.ts` 均无退款数据；`lib/services/adminRefunds.ts` 导出仅列表/详情/start-review/approve/reject，**无任何金额汇总** | 订单详情页的售后摘要可看单笔退款金额，但不是聚合统计 |
| P1 | 管理 | 待处理事项 | `PARTIAL` | 概览 7 卡中有「待审核申请 / 审核中申请」，来自 `countApplicationsByStatus()` 的 `pending`/`reviewing`，是**真实仓储计数**（测试「新建一条申请，待审核申请跟着变」） | ⚠️ **无「待退款 / 待投诉」数字**——退款列表页与投诉列表页各自独立，不向概览回传待办计数。📌 「待处理退款 / 待处理投诉 / 今日消息数」只存在于**客服工作台**（`lib/constants/staff.ts`、`lib/services/staffConversations.ts todayMessageCount`），不属于管理后台概览 |
| P1 | 营销 | 优惠券 | `PARTIAL` | 真实部分：`app/(mobile)/coupons/page.tsx` → `lib/services/coupons.ts queryCouponsForUser()/claimCouponForUser()`（幂等 + 有效期/停用校验）→ `lib/data/couponRepository.ts` → store；接口 `/api/coupons`、`/api/coupons/[id]/claim` | ⚠️ **不参与结算**：`coupons.ts` 注释「没有核销入口，券面值是展示文案」；`lib/services/checkout.ts` 的 `couponDiscountAmount: 0` **硬编码**（注释「P0 没有优惠券：抵扣恒为 0」）。进度表标 🟠 `NEEDS_FIX`「领取/查询已有，**结算未接**」，P0-3 金额域已为它铺路 |
| P1 | 营销 | 活动管理 | `NOT_IMPLEMENTED` | 全仓 `grep activity\|promotion\|campaign\|满减\|促销`：管理端**无活动/促销/满减模块**；`lib/constants/admin.ts ADMIN_NAV_ITEMS` **无活动条目**；`app/api/admin/` 下无对应路由 | 唯一与「活动」相关的是内容模块的**首页活动展示图 banner**（`lib/services/adminBanners.ts` → `selectActivityImageUrl()`），那只是**一张图**，无满减/促销规则能力 |
| P1 | 内容 | 帮助中心 | `PARTIAL` | `app/(mobile)/help/page.tsx` **直接渲染 `PlaceholderPage`**（「帮助文档内容待管理端后续提供」），无任何 service 调用 | 协议内容是真实的（`app/(mobile)/agreements/page.tsx` → `lib/services/agreements.ts listAgreements()` → `pickCurrentAgreements()` 取启用中版本最高；后台 `/admin/content/agreements` 可编辑），但**帮助中心未引用协议**，两者无关联 |

---

## 四、P2 · 27 项

> P2 均为「后期」项。以下 27 项**除 3 项外全部为 `NOT_IMPLEMENTED`**；`package.json` 依赖仅 `next / react / react-dom`——**无图表库、无 Excel 库、无 WebSocket 库**，这是多项判定的旁证。

| 优先级 | 模块 | 功能 | 状态 | 代码证据 | 备注 |
|---|---|---|---|---|---|
| P2 | 数据 | GMV趋势 | `NOT_IMPLEMENTED` | `grep trend/gmv/analytics/statistics/stats/chart/recharts`：唯一「趋势」命中是 `lib/constants/admin.ts:121` 的注释「看不出趋势的数字」；`lib/constants/admin.ts:233` 把「数据统计图表」列入 `ADMIN_UPCOMING_MODULES`（后续开放、无 href） | 后台概览只聚合 7 个数字，无任何营业额/趋势 |
| P2 | 数据 | 订单趋势 | `NOT_IMPLEMENTED` | 同上关键词；管理端订单页为只读列表/筛选 | — |
| P2 | 数据 | 客单价 | `NOT_IMPLEMENTED` | `grep 客单价\|平均\|avg`：唯一「平均」是陪玩 `rating` 平均分（`lib/types/companion.ts:88`） | 无每单平均消费 |
| P2 | 数据 | 退款率 | `NOT_IMPLEMENTED` | `grep 退款率` 无命中；退款模块（申请/审核/拒绝）存在但**无任何「率」的聚合** | — |
| P2 | 数据 | 用户复购率 | `NOT_IMPLEMENTED` | `grep 复购\|repeat\|回购\|老客\|新客`：`repeat` 命中仅为星级渲染 `"★".repeat()` | — |
| P2 | 数据 | 用户留存 | `NOT_IMPLEMENTED` | `grep retention\|cohort\|留存` **无匹配** | — |
| P2 | 数据 | 用户生命周期价值 | `NOT_IMPLEMENTED` | `grep ltv\|lifecycle\|生命周期\|终身价值`：「生命周期」仅出现在无关注释（如 `lib/types/earning.ts:7`） | — |
| P2 | 数据 | 游戏销售排行 | `NOT_IMPLEMENTED` | `grep salesRank\|销量\|销售排行\|salesCount` **无匹配**；商品只有种子写死的静态 `monthlySales`（`catalogSeed.ts`），后台商品表只读展示「月销」 | **无按游戏维度的销售排行** |
| P2 | 数据 | 商品销售排行 | `NOT_IMPLEMENTED` | 同上 | `monthlySales` 是**静态 mock 数**，不是聚合排行；后台商品列表按排序权重/筛选，无「按销量排序」 |
| P2 | 数据 | 高峰时间分析 | `NOT_IMPLEMENTED` | `grep 高峰\|peak\|时段`：「时段」是商品规格文案（「全天包时段」），非下单时间分布 | — |
| P2 | 排行榜 | 周榜/月榜 | `PARTIAL` | `lib/constants/rankingPeriods.ts` 定义 **`today/yesterday/week/month/lastMonth/all` 六周期** + **UTC+8 北京时间边界** + 左闭右开区间 + 纯函数（`now` 由调用方传入）；`lib/services/rankings.ts` 服务端聚合；`app/api/rankings/consumption/route.ts`；页面 `app/(mobile)/rank/page.tsx` + `components/rank/RankingBoard.tsx` | ⚠️ 周期榜**逻辑本身完整且已实现**，但只在**用户消费榜**上成立——**没有游戏/商品/打手榜**，因此「排行榜增加周期」对这一项只能算部分成立 |
| P2 | 排行榜 | 综合打手排行榜 | `NOT_IMPLEMENTED` | 全仓唯一榜单是上一项的用户消费榜；`grep leaderboard\|综合` 无匹配 | 完成率/评价/投诉率三维度**均无** |
| P2 | 打手 | 打手等级 | `NOT_IMPLEMENTED` | `grep 金牌\|银牌\|grade`：命中的「金牌老板」是**用户消费等级**名（`lib/mocks/fixtures/levelSeed.ts:51`）；陪玩仅有展示字符串 `rankLabel`（如「钻石打手」，`lib/types/companion.ts:70`，**纯文案，无等级体系/升级逻辑**） | ⚠️ 勿与 `lib/services/levels.ts` 混淆——那是**用户消费等级**，不是打手等级 |
| P2 | 打手 | 绩效体系 | `NOT_IMPLEMENTED` | `grep 绩效\|接单率\|取消率\|投诉率\|完成率` **零命中**；陪玩仅有 `rating / completedOrderCount / reviewCount / tipsCount` 四个静态数 | 无任何比率聚合 |
| P2 | 派单 | 智能推荐打手 | `NOT_IMPLEMENTED` | 结算页「推荐陪玩」只是把**全部在架陪玩**列出来让用户自己选（`components/checkout/CompanionSheet.tsx`）；`lib/services/companionDispatch.ts:52` 明文「V1 **不做**游戏过滤、商品过滤、等级匹配与智能推荐」 | — |
| P2 | 派单 | 智能派单 | `NOT_IMPLEMENTED` | 派单（P0-5）**已实现但非智能**：`lib/types/dispatch.ts` 是「专属池 → 公共池 → 打手**手动接单**」模型，平台不自动指派/推荐给特定打手 | 同 `companionDispatch.ts:52` 注释 |
| P2 | 营销 | 会员体系 | `PARTIAL` | 存在用户「消费等级」体系：`lib/types/level.ts`、`lib/constants/levels.ts`、`app/api/me/consumption-level/route.ts`、`app/(mobile)/rights/page.tsx`（`LevelListView` + `LevelSummaryCard` + `PrivilegeList`） | ⚠️ `lib/types/level.ts` 与 `levels.ts` 明确权益**只做说明性展示，不实现折扣/优先派单/赠券/返现**；且**后台无等级配置入口**（`lib/constants/admin.ts:231` 把「消费等级与优惠券配置」列为后续开放）。**无 VIP 会员优惠** |
| P2 | 营销 | 积分系统 | `NOT_IMPLEMENTED` | `grep 积分\|points` **零匹配** | ⚠️ 「鸡腿」是打赏计数（`lib/types/tip.ts`，只记录不结算），**不是积分** |
| P2 | 营销 | 邀请奖励 | `NOT_IMPLEMENTED` | `grep invite\|referral\|邀请` **零匹配** | — |
| P2 | 营销 | 用户召回 | `NOT_IMPLEMENTED` | `grep 召回` **零匹配** | 通知系统与优惠券都已存在，但**无「长期未下单 → 发优惠」的召回逻辑** |
| P2 | 财务 | 财务报表 | `NOT_IMPLEMENTED` | `grep 财务报表\|应付\|平台收入\|对账\|finance`：无平台收入/打手应付/退款汇总报表页 | 「应付」仅订单金额展示（`components/checkout/CheckoutForm.tsx:437`） |
| P2 | 财务 | Excel导出 | `NOT_IMPLEMENTED` | `grep xlsx\|excel\|csv\|导出`：`export` 命中**全部是 TS 关键字噪音**；`package.json` **无任何 Excel 库**；无导出 API/按钮 | — |
| P2 | 风控 | 打手自动处罚 | `NOT_IMPLEMENTED` | `grep penalty\|punish\|处罚` **零匹配**；只有**手动**暂停/启用/移除（`/api/admin/companions/[id]/pause\|disable\|remove`） | ⚠️ `Earning.fineAmount` 字段存在但代码注释标明「**无写入路径**」（`lib/types/earning.ts:87`）——只有字段，没有处罚逻辑 |
| P2 | 风控 | 异常用户识别 | `NOT_IMPLEMENTED` | `grep risk\|fraud\|恶意\|异常用户\|风控\|blacklist`：「恶意投诉」仅出现在**协议文案**（`lib/mocks/fixtures/agreementSeed.ts`） | 无任何识别/风控逻辑 |
| P2 | 客服 | 多客服工单 | `NOT_IMPLEMENTED` | `grep ticket\|assign\|transfer\|工单\|转交\|负责人`：`ticket` 命中是请求竞态守卫 `ticketRef`；投诉有 `handledById/handledByRole`（谁最后处理）但**无分配/转交**；`lib/constants/staff.ts:124` 明文「退款与投诉没有『分配给谁』……**本阶段不做工单派发**」 | 客服投诉流是「开始处理 → 解决/关闭」，无转交/负责人分配 |
| P2 | 客服 | SLA统计 | `NOT_IMPLEMENTED` | `grep sla\|响应时长\|时效\|超时率\|响应时间`：`sla` 命中**全部是 `statusLabel` 子串误报**，零真实匹配 | 客服概览有「今日消息数」，那是**计数不是 SLA** |
| P2 | 聊天 | 完整聊天系统 | `PARTIAL` | 已实现：**文字消息 + 历史记录 + 已读/未读 + 客服介入**——用户端 `app/(mobile)/service/chat/[orderId]` + `components/service/OrderChat.tsx`；客服端 `app/staff/(console)/conversations/**` + `lib/services/staffConversations.ts sendMessageForStaff()`（服务端写 `senderRole=customer_service`） | **缺失**：①**图片/附件**——消息类型仅 `body: string`（`lib/types/message.ts`），`grep upload\|image\|attach\|media` 零匹配；②**非实时**——`OrderChat.tsx` 与 `lib/services/conversations.ts:21` 明文「**不是实时聊天，没有 WebSocket**」，靠手动刷新；③**打手未加入**订单聊天（进度表 ⏳ `PLANNED`） |

---

## 五、统计

### 5.1 各优先级状态分布

| 优先级 | 总数 | `DONE` | `PARTIAL` | `NOT_IMPLEMENTED` | `AWAITING_ACCEPTANCE` |
|---|---|---|---|---|---|
| **P0** | 57 | **50 · 87.7%** | **5 · 8.8%** | **2 · 3.5%** | 0 · 0% |
| **P1** | 38 | **5 · 13.2%** | **5 · 13.2%** | **28 · 73.7%** | 0 · 0% |
| **P2** | 27 | **0 · 0%** | **3 · 11.1%** | **24 · 88.9%** | 0 · 0% |
| **合计** | **122** | **55 · 45.1%** | **13 · 10.7%** | **54 · 44.3%** | 0 · 0% |

> 📌 百分比之和在 P1 / 合计行出现 0.1% 的舍入溢出（13.2+13.2+73.7=100.1；45.1+10.7+44.3=100.1），为四舍五入所致，非计数错误。
> 📌 `AWAITING_ACCEPTANCE` 为 0 的理由见文首「一处重要口径说明」——四轮的验收与提交**均已完成**，
> 且其档案 `Status` 已于 2026-09-24 纯文档收口为 `DONE`，与本表的判定**不再有任何差异**。

### 5.2 整体完成率

| 口径 | 计算 | 结果 |
|---|---|---|
| **严格完成率**（只把 `DONE` 算完成） | 55 / 122 | **45.1%** |
| **加权完成率**（`DONE` 计 1、`PARTIAL` 计 0.5） | (55 + 13×0.5) / 122 = 61.5 / 122 | **50.4%** |
| **P0 严格完成率** | 50 / 57 | **87.7%** |
| **P0 加权完成率** | (50 + 5×0.5) / 57 = 52.5 / 57 | **92.1%** |

**读法**：P0 主线（交易闭环）已基本打通——**87.7%** 完成，且剩下的 5 项 `PARTIAL` 中有 4 项是「Mock / 免审批新规 / 金额裁定权」的**口径问题**而非链路缺失；真正的 P0 硬缺口只有 **2 项**（客服查看订单、客服更换打手）。**P1 与 P2 基本是空白区**（P1 完成 13.2%，P2 完成 0%），符合它们「推荐 / 后期」的定位。

### 5.3 P0 尚未完成项目清单（7 项）

| # | 模块 | 功能 | 状态 | 缺什么 |
|---|---|---|---|---|
| 1 | 客服 | **查看订单** | `NOT_IMPLEMENTED` | 客服端**完全没有**订单查询/详情能力（无页面、无接口，被接口清单门禁钉死为「不存在」）。只能从会话/投诉/退款/完成材料间接看到裁剪摘要 |
| 2 | 客服 | **更换打手** | `NOT_IMPLEMENTED` | 只有 `serving→paid` 结构位与 `staff_reassign` 类型，**零入口、零调用点**。连带「打手封禁订单联动」「换人后聊天隔离」也未实现 |
| 3 | 售后 | **已接单未服务退款** | `PARTIAL` | 只有「用户申请 → 管理员审批」。**2026-09-23 新规要求的 `paid/accepted` 免审批直接全额退款未实现**（进度表 🟠 `NEEDS_FIX`） |
| 4 | 售后 | **管理员退款处理** | `PARTIAL` | 能否决/通过，但**金额不可由管理员决定**（取订单实付快照）、**不支持部分退款**。「管理员决定最终退款金额」这一句不成立 |
| 5 | 用户 | **微信内登录** | `PARTIAL` | 只有 Mock 身份选择。无 `openid / jscode2session / 公众号 OAuth` 任何实现 |
| 6 | 支付 | **微信支付** | `PARTIAL` | 只有 Mock 支付通道（成功/失败/取消三个按钮）。无真实微信支付、无任何凭据 |
| 7 | 首页 | **活动入口** | `PARTIAL` | 首页活动图来自后台 banner（真实配置），但 `/activities` 是**占位页**；快捷入口「点单权益」指向 `/placeholder`。原表建议列为「建议有」，非必须 |

> P0 中**必须（必须）**且完全未实现的只有 **#1 客服查看订单** 与 **#2 客服更换打手** 两项。

---

## 六、最适合下一批开发的功能清单

排序依据：**P0 硬缺口优先 → 同族一次做完 → 有既成基础设施可直接复用 → 越靠前越能解锁其他功能**。

### 第 1 批（建议立刻做：补齐 P0 客服侧 + 退款新规）

| 序 | 功能 | 优先级 | 为什么现在做 | 可复用的既有基础 |
|---|---|---|---|---|
| 1 | **客服通用订单查询与详情**（列表 + 按订单号/状态/游戏筛选 + 详情，字段按客服视界裁剪） | P0 必须 | 唯一「必须且零实现」的客服能力。没有它，客服只能被动从会话/投诉里看单 | `lib/services/adminOrders.ts` 的解析与筛选可平移；`lib/constants/staff*.ts` 已有 `toStaffOrderSummary` 裁剪函数可直接复用 |
| 2 | **客服更换打手**（`serving/accepted → paid → public` 重新派单） | P0 必须 | 第二个「必须且零实现」。需求已冻结（不设次数上限、由客服判断） | 结构位已备（`ORDER_TRANSITIONS`）、写入原语已备（`writeAcceptanceRelease` / `appendCompanionRelease` / `applyDispatchToPublic`），**只差入口与 `staff_reassign` 这一条 source 的接线** |
| 3 | **打手封禁订单联动**（`enabled=false` → accepted/serving 回公共池 + 通知 + 旧完成材料失效） | P0 关联 | 与 #2 **同一族、同一组原语**，一起做增量很小；单独做则要重复踩一遍 | 同上；`completionTransaction.ts` 的 stale-submission 防御**已经写好** |
| 4 | **`paid/accepted` 免审批直接全额退款**（`→ refunded`，打手收益为 0、不生成 Earning、通知打手） | P0 必须（新规） | 进度表 🟠 `NEEDS_FIX` 挂账中；改的是既有退款链的口径，越晚改历史包袱越大 | `applyOrderRefund()`、退款状态机、`REFUNDABLE_ORDER_STATUSES` 全在 |

### 第 2 批（P1 里性价比最高的两组「一次做完一族」）

| 序 | 功能 | 优先级 | 为什么 | 可复用 |
|---|---|---|---|---|
| 5 | **老板订单高级查询**（时间/游戏/商品/打手/金额 + 再来一单） | P1 推荐（原表 5 项 `推荐` + 1 项 `可选`） | 一次把 6 个 P1 项从 0 做到 1，是全表 ROI 最高的一批 | `OrderListQuery` 与数据层 `queryOrders()` 的过滤是**同一处**，加参数即通；「再来一单」只需把历史单的 productId/specId 回填结算页 |
| 6 | **管理后台「今日」口径统计**（今日订单数 / 成交额 / 退款金额 / 待处理事项汇总） | P1 推荐（4 项） | 一次补齐 4 项；当前概览页 7 张卡**全是累计**，运营看不到当天 | `getAdminOverview()` 的并发读骨架、`adminOrders`/`adminRefunds`/`adminComplaints` 的服务函数都在，只需加日期过滤与金额聚合 |
| 7 | **打手数据面板**（今日/本月接单数、今日/本月收入、完成率、取消率） | P1 推荐（6 项） | 打手端概览页**目前是纯静态壳**（自陈不读任何数据），性价比高 | `earningTransaction` / `queryOrdersByCompanion` 已能取到所需数据，只差聚合 |
| 8 | **老板数据面板**（累计订单数、累计消费、近 30 天消费、常玩游戏、常用打手） | P1 推荐（4 项 `推荐` + 1 项 `可选`） | 与 #7 同构，可并行 | `sumEffectiveSpend()` / `countEffectiveOrders()` **已经写好但没外发** |
| 9 | **打手排行榜族**（接单榜 / 完成榜 / 收入榜 + 周月周期） | P1 推荐（3 项 + P2 周月榜） | 周期逻辑**已完整实现**（六周期 + UTC+8 边界 + 纯函数 + 测试），换聚合维度即可复用 | `lib/constants/rankingPeriods.ts` + `RankingBoard` 组件 |
| 10 | **优惠券参与结算抵扣** | P1 可选（但进度表标 🟠 `NEEDS_FIX`） | 券的「领」已经有，「用」没有——现在券是**纯展示** | P0-3 金额域**已预留** `couponDiscountAmount`（现硬编码 0），是明写的接入点 |

### 第 3 批（上线前必须解决的非功能项，建议单独立轮）

| 序 | 事项 | 为什么必须 |
|---|---|---|
| 11 | **真实调度器（Scheduler）** | 目前三处「到期自动」全靠**读路径惰性物化**（`sweepExpiredDispatches` / `sweepCompletionAutoApprovals` / `sweepMaturedEarnings`）——**没有人访问就不会发生**。自动退款、自动完成、收益解冻在生产上都要求「到点即发生」 |
| 12 | **微信内登录（公众号 OAuth）+ 微信支付** | 上线硬门槛。当前均为 Mock；进度表已列 `PLANNED`（需认证服务号 / 商户号） |
| 13 | **移除用户端 DTO 上的 `companionRateSnapshot`** | 已知技术债：分账比例虽**未在页面渲染**，但**确实出现在用户端订单详情响应里**。上线前移除 |
| 14 | **打手加入订单聊天 + 换人后聊天隔离** | 进度表 ⏳ `PLANNED`；换人（#2）落地后紧接着就必须做隔离，否则新旧打手会看到彼此的会话 |

### 不建议现在做

**P2 全部 27 项**（除已有的两项半）**建议继续后置**：它们依赖「有真实数据量」才说得通（趋势、留存、复购、LTV、高峰时段），且当前**无数据仓库、无真实支付、无调度器**——现在做出来的只是**又一层 Mock**。其中唯一值得提前想清楚的是 **`排行榜周榜/月榜`**（基础设施已就绪，成本极低）与 **`完整聊天系统` 的图片/附件**（它是客服换人后取证能力的实际前提）。

---

## 附：审计的只读性说明

- **未修改任何代码**：`lib/` `app/` `components/` `tests/` 下文件在本审计期间**零改动**。
- **未修改任何既有文档**：`docs/` 下既有文件**零改动**；本文件是本次审计**唯一**新增产物。
- **零 Git 写操作**：无 `add` / `commit` / `push` / `stash` / `checkout` 等任何写命令；HEAD 始终为 `eef4e62`。只读检查结束时 `git status --short` **零行**（工作区 clean）；本文件写入后，`git status` 新增的**唯一**条目就是本文件自身（`?? docs/03-dev/需求功能点进度表.md`），**没有任何既有文件被改动**。
- **证据可回溯**：本表每一行的「代码证据」列均可直接定位到文件；凡无证据者已明确写「未找到 / 零命中 / 完全无」，未按需求文档推断。
